/**
 * 자석스티커 컴팩트 시스템 CSS - 프리미엄 디자인
 * 강력한 자석 스티커 제품을 위한 전용 스타일링
 * Created: 2025년 8월 (AI Assistant - Frontend Persona)
 */

/* 🧲 자석스티커 전용 변수 */
:root {
    --magnet-primary: #2c5aa0;
    --magnet-secondary: #1e3a8a;
    --magnet-accent: #3b82f6;
    --magnet-success: #10b981;
    --magnet-warning: #f59e0b;
    --magnet-gradient: linear-gradient(135deg, #2c5aa0 0%, #3b82f6 100%);
    --magnet-shadow: 0 4px 20px rgba(44, 90, 160, 0.15);
    --magnet-hover-shadow: 0 8px 30px rgba(44, 90, 160, 0.25);
    --border-radius: 12px;
    --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* 🎨 컨테이너 및 레이아웃 */
.compact-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 2rem;
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    min-height: 100vh;
}

.page-title {
    text-align: center;
    margin-bottom: 2.5rem;
    padding: 2rem;
    background: var(--magnet-gradient);
    border-radius: var(--border-radius);
    color: #333;
    box-shadow: var(--magnet-shadow);
}

.page-title h1 {
    font-size: 2.5rem;
    font-weight: 700;
    margin: 0 0 0.5rem 0;
    text-shadow: 0 1px 2px rgba(255,255,255,0.5);
}

.page-title p {
    font-size: 1.1rem;
    opacity: 0.9;
    margin: 0;
}

/* 📱 메인 그리드 레이아웃 */
.main-content {
    display: grid;
    grid-template-columns: 500px 1fr;
    gap: 2rem;
    align-items: start;
}

@media (max-width: 1200px) {
    .main-content {
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }
}

/* 🖼️ 갤러리 섹션 */
.gallery-section {
    background: white;
    border-radius: var(--border-radius);
    padding: 1.5rem;
    box-shadow: var(--magnet-shadow);
    transition: var(--transition);
    height: fit-content;
    position: sticky;
    top: 2rem;
}

.gallery-section:hover {
    box-shadow: var(--magnet-hover-shadow);
    transform: translateY(-2px);
}

.gallery-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--magnet-primary);
    margin-bottom: 1rem;
    text-align: center;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid #e2e8f0;
}

/* 갤러리 컨텐츠 */
#mstickerGallery {
    min-height: 400px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #f8fafc;
    border-radius: 8px;
    border: 2px dashed #cbd5e1;
}

#mstickerGallery .loading {
    font-size: 1.1rem;
    color: var(--magnet-primary);
    text-align: center;
}

/* 🧮 계산기 섹션 */
.calculator-section {
    background: white;
    border-radius: var(--border-radius);
    padding: 1.5rem;
    box-shadow: var(--magnet-shadow);
    transition: var(--transition);
}

.calculator-section:hover {
    box-shadow: var(--magnet-hover-shadow);
}

.calculator-header {
    display: none !important;
}

.calculator-header h3 {
    display: none !important;
}

/* 📋 옵션 그리드 */
.options-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-bottom: 1.5rem;
}

.option-group {
    display: flex;
    flex-direction: column;
}

.option-group.full-width {
    grid-column: 1 / -1;
}

.option-label {
    font-weight: 600;
    color: var(--magnet-primary);
    margin-bottom: 0.5rem;
    font-size: 0.95rem;
}

.option-select {
    padding: 0.75rem;
    border: 2px solid #e2e8f0;
    border-radius: 8px;
    font-size: 1rem;
    background: white;
    transition: var(--transition);
    cursor: pointer;
}

.option-select:focus {
    outline: none;
    border-color: var(--magnet-accent);
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

.option-select:hover {
    border-color: var(--magnet-accent);
}

.option-select option {
    padding: 0.5rem;
}

/* 💰 가격 표시 */
.price-display {
    background: var(--magnet-gradient);
    color: #333;
    padding: 1.5rem;
    border-radius: var(--border-radius);
    text-align: center;
    margin-bottom: 1.5rem;
    box-shadow: var(--magnet-shadow);
    transition: var(--transition);
}

.price-display:hover {
    transform: scale(1.02);
}

.price-label {
    font-size: 1.1rem;
    font-weight: 500;
    opacity: 0.9;
    margin-bottom: 0.5rem;
}

.price-amount {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    text-shadow: 0 1px 2px rgba(255,255,255,0.5);
}

.price-details {
    font-size: 0.9rem;
    opacity: 0.8;
    font-style: italic;
}

/* 📎 업로드 버튼 */
.upload-order-button {
    text-align: center;
    margin-top: 1rem;
}

.btn-upload-order {
    background: var(--magnet-gradient);
    color: #333;
    border: none;
    padding: 1rem 2rem;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: var(--border-radius);
    cursor: pointer;
    transition: var(--transition);
    box-shadow: var(--magnet-shadow);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
}

.btn-upload-order:hover {
    transform: translateY(-2px);
    box-shadow: var(--magnet-hover-shadow);
}

.btn-upload-order:active {
    transform: translateY(0);
}

/* 📁 업로드 모달 */
.upload-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 10000;
    display: flex;
    align-items: center;
    justify-content: center;
}

.modal-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(5px);
}

.modal-content {
    background: white;
    border-radius: var(--border-radius);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.25);
    max-width: 90vw;
    max-height: 90vh;
    width: 800px;
    overflow: hidden;
    position: relative;
    animation: modalSlideIn 0.3s ease-out;
}

@keyframes modalSlideIn {
    from {
        opacity: 0;
        transform: scale(0.9) translateY(-20px);
    }
    to {
        opacity: 1;
        transform: scale(1) translateY(0);
    }
}

.modal-header {
    padding: 1.5rem;
    border-bottom: 1px solid #e2e8f0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: var(--magnet-gradient);
    color: #333;
}

.modal-title {
    font-size: 1.25rem;
    font-weight: 600;
    margin: 0;
}

.modal-close {
    background: rgba(255, 255, 255, 0.2);
    border: none;
    color: #333;
    font-size: 1.5rem;
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    cursor: pointer;
    transition: var(--transition);
}

.modal-close:hover {
    background: rgba(255, 255, 255, 0.3);
}

.modal-body {
    padding: 1.5rem;
    max-height: 60vh;
    overflow-y: auto;
}

/* 🎯 업로드 컨테이너 */
.upload-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
}

@media (max-width: 768px) {
    .upload-container {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
}

.upload-label {
    font-weight: 600;
    color: var(--magnet-primary);
    margin-bottom: 0.75rem;
    display: block;
    font-size: 1rem;
}

/* 업로드 방법 버튼 */
.upload-buttons {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1rem;
}

.btn-upload-method {
    flex: 1;
    padding: 0.75rem;
    border: 2px solid #e2e8f0;
    background: white;
    border-radius: 8px;
    font-size: 0.9rem;
    cursor: pointer;
    transition: var(--transition);
}

.btn-upload-method:hover {
    border-color: var(--magnet-accent);
}

.btn-upload-method.active {
    background: var(--magnet-accent);
    color: #333;
    border-color: var(--magnet-accent);
}

.btn-upload-method:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

/* 🎯 드래그 앤 드롭 영역 */
.upload-dropzone {
    border: 3px dashed #cbd5e1;
    border-radius: var(--border-radius);
    padding: 2rem;
    text-align: center;
    background: #f8fafc;
    transition: var(--transition);
    cursor: pointer;
    position: relative;
    min-height: 120px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

.upload-dropzone:hover {
    border-color: var(--magnet-accent);
    background: rgba(59, 130, 246, 0.05);
}

.upload-dropzone.dragover {
    border-color: var(--magnet-accent);
    background: rgba(59, 130, 246, 0.1);
    transform: scale(1.02);
}

.upload-icon {
    font-size: 2rem;
    margin-bottom: 0.5rem;
}

.upload-text {
    font-weight: 500;
    color: var(--magnet-primary);
}

.upload-info {
    margin-top: 1rem;
    font-size: 0.8rem;
    color: #64748b;
    line-height: 1.4;
}

/* 📝 메모 입력 */
.memo-textarea {
    width: 100%;
    min-height: 120px;
    padding: 0.75rem;
    border: 2px solid #e2e8f0;
    border-radius: 8px;
    font-family: inherit;
    font-size: 0.9rem;
    resize: vertical;
    transition: var(--transition);
}

.memo-textarea:focus {
    outline: none;
    border-color: var(--magnet-accent);
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

/* 📢 안내사항 */
.upload-notice {
    margin-top: 1rem;
    padding: 1rem;
    background: #fef3c7;
    border-radius: 8px;
    border-left: 4px solid var(--magnet-warning);
}

.notice-item {
    font-size: 0.85rem;
    color: #92400e;
    margin-bottom: 0.5rem;
    line-height: 1.4;
}

.notice-item:last-child {
    margin-bottom: 0;
}

/* 📂 업로드된 파일 목록 */
.uploaded-files {
    margin-top: 1rem;
    padding: 1rem;
    background: #f0f9ff;
    border-radius: 8px;
    border: 1px solid #bae6fd;
}

.uploaded-files h5 {
    margin: 0 0 0.75rem 0;
    color: var(--magnet-primary);
    font-weight: 600;
}

.file-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.file-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.5rem;
    background: white;
    border-radius: 6px;
    border: 1px solid #e0f2fe;
}

.file-name {
    font-size: 0.9rem;
    color: var(--magnet-primary);
    font-weight: 500;
}

.file-size {
    font-size: 0.8rem;
    color: #64748b;
}

/* 🎯 모달 푸터 */
.modal-footer {
    padding: 1rem 1.5rem;
    border-top: 1px solid #e2e8f0;
    background: #f8fafc;
    display: flex;
    justify-content: center;
    gap: 1rem;
}

.modal-btn {
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 8px;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
}

.btn-cart {
    background: var(--magnet-gradient);
    color: #333;
    box-shadow: var(--magnet-shadow);
}

.btn-cart:hover {
    transform: translateY(-1px);
    box-shadow: var(--magnet-hover-shadow);
}

/* 🧲 자석스티커 특수 효과 */
.magnet-pulse {
    animation: magnetPulse 2s infinite;
}

@keyframes magnetPulse {
    0%, 100% {
        transform: scale(1);
        box-shadow: var(--magnet-shadow);
    }
    50% {
        transform: scale(1.05);
        box-shadow: var(--magnet-hover-shadow);
    }
}

/* 📱 반응형 디자인 */
@media (max-width: 768px) {
    .compact-container {
        padding: 1rem;
    }
    
    .page-title {
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }
    
    .page-title h1 {
        font-size: 2rem;
    }
    
    .options-grid {
        grid-template-columns: 1fr;
        gap: 0.75rem;
    }
    
    .price-amount {
        font-size: 1.5rem;
    }
    
    .btn-upload-order {
        padding: 0.875rem 1.5rem;
        font-size: 1rem;
    }
    
    .modal-content {
        width: 95vw;
        margin: 1rem;
    }
    
    .upload-dropzone {
        padding: 1.5rem;
        min-height: 100px;
    }
}

/* 🎨 로딩 상태 */
.loading-spinner {
    display: inline-block;
    width: 1rem;
    height: 1rem;
    border: 2px solid #e2e8f0;
    border-top: 2px solid var(--magnet-accent);
    border-radius: 50%;
    animation: spin 1s linear infinite;
}

@keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}

/* 🧲 자석 효과 애니메이션 */
.magnet-attract {
    animation: magnetAttract 0.6s ease-out;
}

@keyframes magnetAttract {
    0% {
        transform: translateX(-10px) scale(0.9);
        opacity: 0.5;
    }
    50% {
        transform: translateX(5px) scale(1.1);
        opacity: 0.8;
    }
    100% {
        transform: translateX(0) scale(1);
        opacity: 1;
    }
}

/* ✨ 성공 상태 */
.success-state {
    background: var(--magnet-success);
    color: #333;
    animation: successPulse 0.6s ease-out;
}

@keyframes successPulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
}