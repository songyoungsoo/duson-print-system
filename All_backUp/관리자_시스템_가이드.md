# 관리자 로그인 시스템 완전 가이드

## 🔐 관리자 로그인 (admin / admin123)

### 📍 로그인 페이지
```
http://localhost/admin/MlangPrintAuto/login.php
```

**인증 정보**:
- 아이디: `admin`
- 비밀번호: `admin123`
- 인증 방식: 하드코딩 (데이터베이스 사용 안 함)

---

## 🎯 이 로그인이 필요한 페이지

### 1️⃣ **통합 품목 관리 시스템** ⭐ (메인)

**URL**: `http://localhost/admin/product-manager/index.php`

**기능**:
- 8개 품목의 가격표 통합 관리
- 품목: 명함, 전단지, 카다록, 봉투, 포스터, 상품권, 자석스티커, NCR양식
- 가격 검색, 필터링, 수정

**코드 위치**: `/var/www/html/admin/product-manager/index.php` (10-12번 라인)
```php
if (!isset($_SESSION['admin_logged_in']) || $_SESSION['admin_logged_in'] !== true) {
    header('location: ../mlangprintauto/login.php');
    exit;
}
```

**로그인 후 자동 이동**:
- login.php에서 로그인 성공 시 기본 리다이렉트: `../../duson/admin/product-manager/`

---

### 2️⃣ **세금계산서 발급 관리**

**URL**: `http://localhost/admin/MlangPrintAuto/tax_invoice_admin.php`

**기능**:
- 세금계산서 발급/취소
- 세금계산서 목록 조회

**코드 위치**: `/var/www/html/admin/MlangPrintAuto/tax_invoice_admin.php` (10-14번 라인)
```php
if (!isset($_SESSION['admin_logged_in'])) {
    echo "<script>alert('관리자 로그인이 필요합니다.'); location.href='/admin/login.php';</script>";
    exit;
}
```

---

## 📊 인증이 필요없는 관리 페이지

다음 페이지들은 **회원 로그인**(`/member/login.php` - admin/du701018)을 사용하거나 인증이 없습니다:

### 주문 관리
- `/admin/MlangPrintAuto/admin.php` - 주문 수정/관리 (회원 인증 사용)
- `/admin/MlangPrintAuto/orderlist250925.php` - 주문 목록

### 품목 관리 (개별)
- `/admin/MlangPrintAuto/productmanager.php` - 품목 관리 (인증 없음)
- `/admin/MlangPrintAuto/cateadmin.php` - 카테고리 관리
- 각 품목별 개별 관리 페이지들

---

## 🎭 두 가지 관리자 시스템 비교

| 항목 | 상품관리 시스템 | 교정보기 관리 |
|-----|---------------|-------------|
| **로그인 페이지** | `/admin/MlangPrintAuto/login.php` | `/member/login.php` |
| **아이디** | `admin` | `admin` |
| **비밀번호** | `admin123` | `du701018` |
| **인증 방식** | 하드코딩 (세션) | 데이터베이스 (member 테이블) |
| **세션 변수** | `$_SESSION['admin_logged_in']` | `$_SESSION['username']`, `$_SESSION['user_id']` |
| **주요 기능** | 가격표 관리, 세금계산서 | 전체 고객 주문 교정 확인 |
| **사용 페이지** | `/admin/product-manager/`<br>`/admin/MlangPrintAuto/tax_invoice_admin.php` | `/sub/checkboard.php` (관리자 모드) |

---

## 🚀 사용 가이드

### 상품관리 시스템 접속 순서

1. **로그인**
   ```
   http://localhost/admin/MlangPrintAuto/login.php
   ```
   - 아이디: `admin`
   - 비밀번호: `admin123`

2. **자동 이동** → 통합 품목 관리 시스템
   ```
   http://localhost/admin/product-manager/index.php
   ```

3. **가격표 관리 시작**
   - 품목 선택 (명함, 전단지 등)
   - 가격 검색/수정

---

### 교정보기 관리자 접속 순서

1. **로그인**
   ```
   http://localhost/member/login.php
   ```
   - 아이디: `admin`
   - 비밀번호: `du701018`

2. **교정보기 페이지 접속**
   ```
   http://localhost/sub/checkboard.php
   ```

3. **관리자 권한 동작**
   - 주문 클릭 시 비밀번호 입력 없이 즉시 이미지 확인
   - JavaScript의 `isAdmin` 플래그로 자동 구분

---

## 🔍 세션 변수로 관리자 확인

### 상품관리 시스템
```php
// login.php에서 설정
$_SESSION['admin_logged_in'] = true;
$_SESSION['admin_username'] = 'admin';
$_SESSION['admin_login_time'] = time();

// 다른 페이지에서 확인
if (isset($_SESSION['admin_logged_in']) && $_SESSION['admin_logged_in'] === true) {
    // 접근 허용
}
```

### 교정보기 관리자
```php
// login_unified.php에서 설정
$_SESSION['user_id'] = $user_id;
$_SESSION['username'] = 'admin';
$_SESSION['user_name'] = '관리자';

// checkboard.php에서 확인
$is_admin = false;
if (isset($_SESSION['username']) && $_SESSION['username'] == 'admin') {
    $is_admin = true;
}
```

---

## ⚠️ 주의사항

1. **비밀번호 혼동 주의**
   - 상품관리: `admin123`
   - 교정보기: `du701018`

2. **로그인 페이지 구분**
   - 상품관리: `/admin/MlangPrintAuto/login.php`
   - 교정보기: `/member/login.php`

3. **세션 충돌 없음**
   - 두 시스템은 서로 다른 세션 변수 사용
   - 동시 로그인 가능

4. **보안 개선 필요**
   - 상품관리 시스템: 하드코딩된 비밀번호 → 데이터베이스 기반 인증으로 변경 권장
   - 프로덕션 환경에서는 강화된 보안 필요

---

## 📋 빠른 참조

```bash
# 상품관리 시스템
URL: http://localhost/admin/MlangPrintAuto/login.php
ID: admin
PW: admin123
→ 자동 이동: http://localhost/admin/product-manager/

# 교정보기 관리
URL: http://localhost/member/login.php
ID: admin
PW: du701018
→ 수동 이동: http://localhost/sub/checkboard.php
```
