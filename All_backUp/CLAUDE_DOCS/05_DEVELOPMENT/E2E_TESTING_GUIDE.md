# E2E 테스팅 가이드 (Playwright MCP)

## 📋 개요

Playwright MCP를 활용한 프론트엔드 E2E (End-to-End) 테스트 가이드입니다.
- **도구**: Playwright MCP (@playwright/mcp)
- **대상**: 10개 제품 페이지, 주문 시스템, 회원 시스템
- **환경**: http://localhost (로컬), http://dsp1830.shop (스테이징)

## 🎯 테스트 범위

### 1. 제품 페이지 (10개)
- ✅ 전단지 (inserted)
- ✅ 봉투 (envelope)
- ✅ 명함 (namecard)
- ✅ 스티커 (sticker_new)
- ✅ 자석스티커 (msticker)
- ✅ 카다록 (cadarok)
- ✅ 포스터 (littleprint)
- ✅ 상품권 (merchandisebond)
- ✅ NCR양식 (ncrflambeau)
- ✅ 리플렛 (inserted - leaflet과 동일)

### 2. 주문 프로세스
- ✅ 제품 견적 계산
- ✅ 장바구니 담기
- ✅ 주문서 작성 (회원/비회원)
- ✅ 주문 완료

### 3. 회원 시스템
- ✅ 회원가입
- ✅ 로그인/로그아웃
- ✅ 마이페이지

### 4. 교정확인
- ✅ 주문 조회
- ✅ 전화번호 뒷자리 4자리 검증

## 🚀 Playwright MCP 사용법

### MCP 서버 확인
```bash
# .mcp.json 확인
cat /var/www/html/.mcp.json
```

### Playwright 브라우저 설치
```bash
# Playwright 브라우저 설치
npx playwright install
```

## 📝 E2E 테스트 시나리오

### 시나리오 1: 제품 견적 계산 및 장바구니 담기

**대상**: 전단지 (inserted) 페이지

**테스트 흐름**:
1. 전단지 페이지 접속
2. 옵션 선택:
   - 용지 사이즈 선택
   - 용지 종류 선택
   - 수량 선택
   - 인쇄 옵션 선택
3. 가격 계산 확인
4. 장바구니 담기 버튼 클릭
5. 장바구니 페이지로 이동 확인
6. 장바구니에 상품이 담겼는지 확인

**검증 포인트**:
- [ ] 페이지 로딩 시간 < 3초
- [ ] 가격 계산 AJAX 응답 시간 < 1초
- [ ] `window.currentPriceData` 객체 존재
- [ ] 장바구니 담기 성공 메시지 표시
- [ ] 장바구니 페이지 리다이렉트

### 시나리오 2: 회원가입 프로세스

**대상**: 회원가입 페이지 (member/form.php)

**테스트 흐름**:
1. 회원가입 페이지 접속
2. 필수 항목 입력:
   - 아이디
   - 비밀번호
   - 비밀번호 확인
   - 이름
   - 이메일
   - 전화번호
   - 휴대폰
   - 주소 (Daum API)
3. 사업자 정보 입력 (선택):
   - 사업자등록번호
   - 대표자명
   - 사업장 주소
   - 업태/종목
4. 회원가입 버튼 클릭
5. 가입 완료 확인

**검증 포인트**:
- [ ] 아이디 중복 체크 작동
- [ ] 비밀번호 일치 여부 확인
- [ ] 필수 항목 미입력 시 경고 메시지
- [ ] Daum 주소 API 정상 작동
- [ ] 회원가입 성공 메시지
- [ ] 로그인 페이지 또는 메인 페이지 리다이렉트

### 시나리오 3: 로그인 및 주문 프로세스 (회원)

**대상**: 로그인 → 제품 선택 → 주문 완료

**테스트 흐름**:
1. 로그인 모달 오픈
2. 회원 아이디/비밀번호 입력
3. 로그인 성공 확인
4. 전단지 페이지 접속
5. 견적 계산 후 장바구니 담기
6. 주문서 작성 페이지 이동
7. 회원 정보 자동 입력 확인
8. 배송지 선택 (회원 주소 사용)
9. 주문하기 버튼 클릭
10. 주문 완료 페이지 확인

**검증 포인트**:
- [ ] 로그인 성공 후 사용자명 표시
- [ ] 회원 정보 자동 입력 (이름, 이메일, 전화번호)
- [ ] 회원 주소 자동 입력
- [ ] 주문 완료 후 주문번호 표시
- [ ] 데이터베이스에 `is_member = 1`로 저장

### 시나리오 4: 비회원 주문 프로세스

**대상**: 비로그인 상태 주문

**테스트 흐름**:
1. 비로그인 상태 확인
2. 전단지 페이지 접속
3. 견적 계산 후 장바구니 담기
4. 주문서 작성 페이지 이동
5. 비회원 정보 수동 입력:
   - 이름
   - 이메일
   - 전화번호
   - 휴대폰
   - 배송 주소
6. 주문하기 버튼 클릭
7. 주문 완료 페이지 확인

**검증 포인트**:
- [ ] 비회원 입력 폼 표시
- [ ] 필수 항목 미입력 시 경고
- [ ] 이메일 형식 검증
- [ ] 전화번호 형식 검증
- [ ] 주문 완료 후 주문번호 표시
- [ ] 데이터베이스에 `is_member = 0`로 저장

### 시나리오 5: 교정확인 검증

**대상**: 교정확인 페이지 (sub/checkboard.php)

**테스트 흐름**:
1. 교정확인 페이지 접속
2. 주문 목록 확인
3. 특정 주문 클릭
4. 인증 모달 표시 확인
5. 전화번호 뒷자리 4자리 입력
6. 확인 버튼 클릭
7. 주문 상세 페이지 이동 확인

**검증 포인트**:
- [ ] 주문 목록 정상 표시
- [ ] 인증 모달 정상 표시
- [ ] 잘못된 전화번호 입력 시 오류 메시지
- [ ] 올바른 전화번호 입력 시 주문 상세 페이지 이동
- [ ] 관리자는 인증 없이 바로 접근

## 🛠️ Playwright MCP 도구 사용

### 1. 브라우저 네비게이션

```javascript
// 페이지 접속
await browser_navigate({ url: "http://localhost/mlangprintauto/inserted/" });

// 뒤로 가기
await browser_navigate_back();
```

### 2. 페이지 스냅샷

```javascript
// 페이지 구조 확인
const snapshot = await browser_snapshot();
console.log(snapshot);
```

### 3. 요소 클릭

```javascript
// 요소 클릭
await browser_click({
  element: "용지 사이즈 선택",
  ref: "select#MY_type"
});
```

### 4. 텍스트 입력

```javascript
// 입력 필드에 텍스트 입력
await browser_type({
  element: "이름 입력",
  ref: "input[name='username']",
  text: "홍길동"
});
```

### 5. 폼 채우기

```javascript
// 여러 필드 한번에 채우기
await browser_fill_form({
  fields: [
    { name: "이름", ref: "input[name='username']", type: "textbox", value: "홍길동" },
    { name: "이메일", ref: "input[name='email']", type: "textbox", value: "test@example.com" },
    { name: "전화번호", ref: "input[name='phone']", type: "textbox", value: "02-1234-5678" }
  ]
});
```

### 6. JavaScript 실행

```javascript
// JavaScript 코드 실행
await browser_evaluate({
  function: "() => { return window.currentPriceData; }"
});
```

### 7. 스크린샷

```javascript
// 페이지 스크린샷
await browser_take_screenshot({
  filename: "inserted-page.png",
  fullPage: true
});
```

### 8. 네트워크 요청 확인

```javascript
// 네트워크 요청 목록
const requests = await browser_network_requests();
console.log(requests);
```

### 9. 콘솔 메시지 확인

```javascript
// 콘솔 메시지 (에러 포함)
const messages = await browser_console_messages({ onlyErrors: true });
console.log(messages);
```

## 🎨 실제 테스트 예제

### 예제 1: 전단지 페이지 로딩 테스트

```javascript
// 1. 페이지 접속
await browser_navigate({ url: "http://localhost/mlangprintauto/inserted/" });

// 2. 페이지 스냅샷
const snapshot = await browser_snapshot();

// 3. 스크린샷 캡처
await browser_take_screenshot({
  filename: "inserted-loaded.png",
  fullPage: true
});

// 4. JavaScript 변수 확인
const priceData = await browser_evaluate({
  function: "() => { return typeof window.currentPriceData !== 'undefined'; }"
});

// 5. 콘솔 에러 확인
const errors = await browser_console_messages({ onlyErrors: true });
console.log("콘솔 에러:", errors);
```

### 예제 2: 견적 계산 테스트

```javascript
// 1. 페이지 접속
await browser_navigate({ url: "http://localhost/mlangprintauto/inserted/" });

// 2. 용지 사이즈 선택
await browser_click({
  element: "용지 사이즈",
  ref: "select#MY_type"
});

// 3. A4 선택
await browser_select_option({
  element: "용지 사이즈 선택",
  ref: "select#MY_type",
  values: ["802"]
});

// 4. 용지 종류 선택
await browser_select_option({
  element: "용지 종류 선택",
  ref: "select#PN_type",
  values: ["100"]
});

// 5. 수량 선택
await browser_select_option({
  element: "수량 선택",
  ref: "select#MY_amount",
  values: ["1000"]
});

// 6. 가격 계산 대기 (1초)
await browser_wait_for({ time: 1 });

// 7. 가격 데이터 확인
const priceData = await browser_evaluate({
  function: "() => { return window.currentPriceData; }"
});

console.log("계산된 가격:", priceData);

// 8. 스크린샷 캡처
await browser_take_screenshot({
  filename: "inserted-calculated.png"
});
```

### 예제 3: 장바구니 담기 테스트

```javascript
// 1. 견적 계산 완료 후
// 2. 장바구니 담기 버튼 클릭
await browser_click({
  element: "장바구니 담기 버튼",
  ref: "button.btn-add-to-cart"
});

// 3. 성공 메시지 대기
await browser_wait_for({ time: 2 });

// 4. alert/confirm 처리 (있는 경우)
await browser_handle_dialog({ accept: true });

// 5. 장바구니 페이지 이동 확인
const currentUrl = await browser_evaluate({
  function: "() => { return window.location.href; }"
});

console.log("현재 URL:", currentUrl);

// 6. 스크린샷
await browser_take_screenshot({
  filename: "cart-added.png"
});
```

### 예제 4: 회원가입 테스트

```javascript
// 1. 회원가입 페이지 접속
await browser_navigate({ url: "http://localhost/member/join.php" });

// 2. 폼 채우기
await browser_fill_form({
  fields: [
    { name: "아이디", ref: "input[name='id']", type: "textbox", value: "testuser123" },
    { name: "비밀번호", ref: "input[name='pw']", type: "textbox", value: "test1234!@" },
    { name: "비밀번호 확인", ref: "input[name='pw_confirm']", type: "textbox", value: "test1234!@" },
    { name: "이름", ref: "input[name='name']", type: "textbox", value: "테스트유저" },
    { name: "이메일", ref: "input[name='email']", type: "textbox", value: "test@example.com" },
    { name: "전화번호", ref: "input[name='phone']", type: "textbox", value: "02-1234-5678" },
    { name: "휴대폰", ref: "input[name='hendphone']", type: "textbox", value: "010-1234-5678" }
  ]
});

// 3. 주소 검색 버튼 클릭
await browser_click({
  element: "우편번호 찾기",
  ref: "button[onclick='execDaumPostcode()']"
});

// 4. Daum 주소 팝업 처리 (새 탭)
await browser_tabs({ action: "list" });

// 5. 회원가입 버튼 클릭
await browser_click({
  element: "회원가입",
  ref: "button[type='submit']"
});

// 6. 완료 대기
await browser_wait_for({ time: 3 });

// 7. 스크린샷
await browser_take_screenshot({
  filename: "signup-complete.png"
});
```

## 📊 테스트 체크리스트

### 제품 페이지 공통 체크리스트
- [ ] 페이지 로딩 (< 3초)
- [ ] 갤러리 이미지 표시
- [ ] 견적 계산기 표시
- [ ] 옵션 선택 기능
- [ ] 가격 계산 AJAX
- [ ] 가격 표시 (VAT 포함/미포함)
- [ ] 장바구니 담기 버튼
- [ ] 파일 업로드 모달
- [ ] 추가 옵션 (있는 경우)
- [ ] 반응형 레이아웃 (모바일)

### 주문 프로세스 체크리스트
- [ ] 장바구니 페이지 표시
- [ ] 상품 목록 정확성
- [ ] 가격 계산 정확성
- [ ] 주문서 작성 페이지 이동
- [ ] 회원 정보 자동 입력 (회원)
- [ ] 비회원 정보 수동 입력 (비회원)
- [ ] 필수 항목 검증
- [ ] 사업자 정보 입력 (선택)
- [ ] 주문 완료 페이지
- [ ] 주문번호 표시
- [ ] 이메일 발송 (비동기)

### 회원 시스템 체크리스트
- [ ] 회원가입 폼 표시
- [ ] 아이디 중복 체크
- [ ] 비밀번호 일치 검증
- [ ] 이메일 형식 검증
- [ ] 전화번호 형식 검증
- [ ] Daum 주소 API
- [ ] 회원가입 완료
- [ ] 로그인 모달 표시
- [ ] 로그인 성공/실패
- [ ] 로그아웃
- [ ] 마이페이지 접근

### 교정확인 체크리스트
- [ ] 주문 목록 표시
- [ ] 페이지네이션
- [ ] 주문 클릭 시 모달 표시
- [ ] 전화번호 4자리 입력
- [ ] 검증 성공/실패
- [ ] 주문 상세 페이지 이동
- [ ] 관리자 우회 접근

## 🔍 성능 측정 기준

### 페이지 로딩 시간
- **목표**: < 3초
- **측정**: Time to Interactive (TTI)

### AJAX 응답 시간
- **목표**: < 1초
- **측정**: Network Request Duration

### 이미지 로딩 시간
- **목표**: < 2초
- **측정**: Largest Contentful Paint (LCP)

### JavaScript 실행 시간
- **목표**: < 500ms
- **측정**: Total Blocking Time (TBT)

## 🐛 자주 발생하는 문제

### 1. AJAX 타이밍 이슈
**증상**: 가격 계산 후 즉시 장바구니 담기 시 가격이 0원

**해결**:
```javascript
// 가격 계산 대기
await browser_wait_for({ time: 1 });

// 가격 데이터 확인
const priceData = await browser_evaluate({
  function: "() => { return window.currentPriceData !== null; }"
});
```

### 2. 모달 팝업 처리
**증상**: Daum 주소 API 팝업이 새 탭으로 열림

**해결**:
```javascript
// 탭 목록 확인
const tabs = await browser_tabs({ action: "list" });

// 새 탭 선택
await browser_tabs({ action: "select", index: 1 });

// 작업 후 원래 탭으로 복귀
await browser_tabs({ action: "select", index: 0 });
```

### 3. 세션 유지 문제
**증상**: 로그인 후 다른 페이지 이동 시 로그아웃됨

**해결**:
```javascript
// 브라우저 재시작 없이 유지
// Playwright는 기본적으로 세션 쿠키 유지
```

### 4. 파일 업로드
**증상**: 파일 업로드 모달에서 파일 선택이 안됨

**해결**:
```javascript
// 파일 업로드
await browser_file_upload({
  paths: ["/absolute/path/to/test-file.jpg"]
});
```

## 📝 테스트 리포트 형식

### 테스트 결과 템플릿

```markdown
# E2E 테스트 리포트

**테스트 날짜**: 2025-01-31
**환경**: http://localhost
**브라우저**: Chromium

## 테스트 결과 요약

| 시나리오 | 결과 | 소요 시간 | 비고 |
|---------|------|----------|------|
| 전단지 견적 계산 | ✅ PASS | 3.2s | - |
| 장바구니 담기 | ✅ PASS | 2.1s | - |
| 회원가입 | ❌ FAIL | 5.4s | 주소 API 오류 |
| 회원 주문 | ✅ PASS | 8.7s | - |
| 비회원 주문 | ✅ PASS | 9.2s | - |
| 교정확인 검증 | ✅ PASS | 2.3s | - |

## 상세 결과

### 1. 전단지 견적 계산
- **결과**: ✅ PASS
- **소요 시간**: 3.2s
- **검증 항목**:
  - [x] 페이지 로딩 < 3초
  - [x] 가격 계산 정확성
  - [x] window.currentPriceData 존재

### 2. 회원가입
- **결과**: ❌ FAIL
- **소요 시간**: 5.4s
- **오류 내용**: Daum 주소 API 스크립트 로딩 실패
- **스크린샷**: signup-error.png
- **조치 사항**: 네트워크 연결 확인 필요

## 발견된 이슈

1. **주소 API 로딩 실패**
   - **심각도**: 높음
   - **재현 방법**: 회원가입 페이지 → 주소 검색 버튼 클릭
   - **오류 메시지**: "Failed to load resource: net::ERR_FAILED"

2. **가격 계산 딜레이**
   - **심각도**: 낮음
   - **재현 방법**: 옵션 빠르게 변경 시
   - **현상**: 이전 가격이 잠깐 표시됨

## 권장 사항

- [ ] Daum 주소 API CDN 안정성 확인
- [ ] 가격 계산 debounce 추가
- [ ] 로딩 인디케이터 추가
```

## 🔗 관련 문서

- [CLAUDE.md](../CLAUDE.md) - 프로젝트 개요
- [FRONTEND_UI.md](./FRONTEND_UI.md) - 프론트엔드 UI 가이드
- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) - 문제 해결 가이드
- [MCP_Installation_Guide.md](./MCP_Installation_Guide.md) - MCP 설치 가이드

---

**작성일**: 2025-01-31
**작성자**: Claude Code
**버전**: 1.0
