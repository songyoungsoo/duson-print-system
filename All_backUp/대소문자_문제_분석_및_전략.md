# 대소문자 문제 분석 및 해결 전략

## 📋 현황 분석

### 문제의 근본 원인
**XAMPP (Windows) → WSL (Linux) 마이그레이션 과정**
- Windows: 파일시스템 대소문자 무시 (`NameCard.php` = `namecard.php`)
- Linux: 파일시스템 대소문자 구분 (`NameCard.php` ≠ `namecard.php`)

### 현재 상태 (2025-11-08 기준)

#### 1. 데이터베이스 테이블명 (✅ 완전히 소문자로 통일됨)
```
mlangprintauto_cadarok          ✅ 소문자
mlangprintauto_envelope         ✅ 소문자
mlangprintauto_inserted         ✅ 소문자
mlangprintauto_littleprint      ✅ 소문자
mlangprintauto_merchandisebond  ✅ 소문자
mlangprintauto_msticker         ✅ 소문자
mlangprintauto_namecard         ✅ 소문자
mlangprintauto_ncrflambeau      ✅ 소문자
mlangprintauto_sticker          ✅ 소문자
mlangprintauto_transactioncate  ✅ 소문자
```

#### 2. 파일명 패턴 (⚠️ 일관성 없음)

**패턴 A: 완전 소문자 (inserted 스타일)**
```
inserted_admin.php       ✅ 소문자
inserted_NoFild.php      ✅ 소문자
inserted_Script.php      ✅ 소문자
inserted_ScriptSearch.php ✅ 소문자

cadarok_admin.php        ✅ 소문자
cadarok_NoFild.php       ✅ 소문자
cadarok_Script.php       ✅ 소문자

envelope_admin.php       ✅ 소문자
envelope_NoFild.php      ✅ 소문자
envelope_Script.php      ✅ 소문자

sticker_admin.php        ✅ 소문자
sticker_NoFild.php       ✅ 소문자
sticker_Script.php       ✅ 소문자
```

**패턴 B: 혼합 케이스 (CamelCase 스타일)**
```
NameCard_admin.php           ⚠️ 대문자 시작
namecard_NoFild.php          ⚠️ 소문자 (불일치!)
NameCard_Script.php          ⚠️ 대문자 시작

NcrFlambeau_admin.php        ⚠️ 대문자 시작
NcrFlambeau_NoFild.php       ⚠️ 대문자 시작
NcrFlambeau_Script.php       ⚠️ 대문자 시작

MerchandiseBond_admin.php    ⚠️ 대문자 시작
MerchandiseBond_NoFild.php   ⚠️ 대문자 시작
MerchandiseBond_Script.php   ⚠️ 대문자 시작

LittlePrint_admin.php        ⚠️ 대문자 시작
LittlePrint_NoFild.php       ⚠️ 대문자 시작
LittlePrint_Script.php       ⚠️ 대문자 시작
```

#### 3. ConDb.php의 $Ttable 매핑 (⚠️ 혼합)
```php
$Ttable = "inserted"        → 소문자
$Ttable = "msticker"        → 소문자
$Ttable = "sticker"         → 소문자
$Ttable = "NameCard"        → 대문자 시작 ⚠️
$Ttable = "MerchandiseBond" → 대문자 시작 ⚠️
$Ttable = "envelope"        → 소문자
$Ttable = "NcrFlambeau"     → 대문자 시작 ⚠️
$Ttable = "cadarok"         → 소문자
$Ttable = "LittlePrint"     → 대문자 시작 ⚠️
```

---

## 🎯 전략 옵션 분석

### 옵션 1: 완전 소문자 통일 (권장 ⭐⭐⭐⭐⭐)

**장점:**
- ✅ Linux 환경에 최적화
- ✅ 대소문자 오류 완전 제거
- ✅ 일관성 100% 확보
- ✅ 향후 유지보수 용이
- ✅ 새 개발자도 쉽게 이해

**단점:**
- ❌ 모든 파일명 변경 필요 (약 30개 파일)
- ❌ ConDb.php 매핑 수정 필요
- ❌ 작업량 多 (하지만 한 번만 하면 됨)
- ❌ 기존 코드 참조 모두 수정

**작업량:**
```
파일명 변경: 30개 파일
  NameCard_* → namecard_*
  NcrFlambeau_* → ncrflambeau_*
  MerchandiseBond_* → merchandisebond_*
  LittlePrint_* → littleprint_*

코드 수정: 50개 파일 (대략)
  include 문장
  ConDb.php 매핑
  기타 참조
```

**리스크:**
- 🟡 중간 정도 (체계적으로 하면 안전)

---

### 옵션 2: ConDb.php 매핑 테이블 개선 (현재 방식 개선)

**개념:** 파일명은 그대로 두고, 매핑 정보를 중앙 관리

```php
// ConDb.php에 매핑 테이블 추가
$PRODUCT_CONFIG = [
    'inserted' => [
        'table' => 'mlangprintauto_inserted',
        'admin_file' => 'inserted_admin.php',
        'nofild_file' => 'inserted_NoFild.php',
        'script_file' => 'inserted_Script.php',
        'ttable' => 'inserted',
        'display_name' => '전단지'
    ],
    'namecard' => [
        'table' => 'mlangprintauto_namecard',
        'admin_file' => 'NameCard_admin.php',
        'nofild_file' => 'namecard_NoFild.php',  // 실제 파일명
        'script_file' => 'NameCard_Script.php',
        'ttable' => 'NameCard',  // ConDb.php에서 사용
        'display_name' => '명함'
    ],
    // ... 각 제품별 설정
];

// admin.php에서 사용
$product_key = 'namecard';  // URL 파라미터
$config = $PRODUCT_CONFIG[$product_key];
$TABLE = $config['table'];
if($code=="Modify"){include"./{$config['nofild_file']}";}
include"{$config['script_file']}";
```

**장점:**
- ✅ 파일명 변경 불필요
- ✅ 중앙에서 관리 용이
- ✅ 현재 작동 중인 코드 유지
- ✅ 새 제품 추가 시 명확한 가이드

**단점:**
- ❌ 복잡도 증가
- ❌ 근본 문제 해결 아님 (우회)
- ❌ 여전히 대소문자 혼재

**작업량:**
```
ConDb.php: 매핑 테이블 추가 (100줄)
각 admin.php: 설정 참조 방식으로 변경 (10개 파일)
```

**리스크:**
- 🟢 낮음 (점진적 적용 가능)

---

### 옵션 3: 하이브리드 (현재 하드코딩 유지)

**개념:** 지금처럼 각 파일에서 하드코딩, 문제 발생 시 개별 해결

**장점:**
- ✅ 추가 작업 불필요
- ✅ 현재 작동 중

**단점:**
- ❌ 유지보수 어려움
- ❌ 일관성 없음
- ❌ 새 제품 추가 시 혼란
- ❌ 버그 재발 가능성

**작업량:**
```
없음 (현상 유지)
```

**리스크:**
- 🔴 높음 (지속적인 문제 발생 예상)

---

## 💡 최종 권장 전략

### 🏆 권장: 옵션 1 (완전 소문자 통일) + 단계적 실행

#### 이유:
1. **근본 해결**: 대소문자 문제를 영원히 제거
2. **Linux 표준**: 소문자가 Linux/Unix 표준 규칙
3. **확장성**: 향후 10년 사용 가능한 구조
4. **유지보수**: 누가 봐도 명확한 구조

#### 단계적 실행 계획 (3단계)

**Phase 1: 준비 및 백업 (1일)**
```bash
# 1. 전체 백업
tar -czf backup_before_lowercase_$(date +%Y%m%d).tar.gz \
    /var/www/html/admin/mlangprintauto \
    /var/www/html/mlangprintauto

# 2. 파일 목록 작성
find /var/www/html/admin/mlangprintauto -name "*_admin.php" > files_to_rename.txt
find /var/www/html/admin/mlangprintauto -name "*_NoFild.php" >> files_to_rename.txt
find /var/www/html/admin/mlangprintauto -name "*_Script.php" >> files_to_rename.txt

# 3. 영향 받는 코드 검색
grep -r "NameCard" /var/www/html/admin/mlangprintauto/*.php > references.txt
grep -r "NcrFlambeau" /var/www/html/admin/mlangprintauto/*.php >> references.txt
grep -r "MerchandiseBond" /var/www/html/admin/mlangprintauto/*.php >> references.txt
grep -r "LittlePrint" /var/www/html/admin/mlangprintauto/*.php >> references.txt
```

**Phase 2: 파일명 변경 (1일)**
```bash
# 스크립트로 자동 변환
cd /var/www/html/admin/mlangprintauto

# NameCard → namecard
mv NameCard_admin.php namecard_admin.php
mv NameCard_List.php namecard_list.php
mv NameCard_Script.php namecard_script.php
mv NameCard_ScriptSearch.php namecard_scriptsearch.php

# NcrFlambeau → ncrflambeau
mv NcrFlambeau_admin.php ncrflambeau_admin.php
mv NcrFlambeau_List.php ncrflambeau_list.php
mv NcrFlambeau_NoFild.php ncrflambeau_nofild.php
mv NcrFlambeau_Script.php ncrflambeau_script.php
mv NcrFlambeau_ScriptSearch.php ncrflambeau_scriptsearch.php

# MerchandiseBond → merchandisebond
mv MerchandiseBond_admin.php merchandisebond_admin.php
mv MerchandiseBond_List.php merchandisebond_list.php
mv MerchandiseBond_NoFild.php merchandisebond_nofild.php
mv MerchandiseBond_Script.php merchandisebond_script.php
mv MerchandiseBond_ScriptSearch.php merchandisebond_scriptsearch.php

# LittlePrint → littleprint
mv LittlePrint_admin.php littleprint_admin.php
mv LittlePrint_List.php littleprint_list.php
mv LittlePrint_NoFild.php littleprint_nofild.php
mv LittlePrint_Script.php littleprint_script.php
mv LittlePrint_ScriptSearch.php littleprint_scriptsearch.php

# cadarokTwo → cadaroktwo
mv cadarokTwo_admin.php cadaroktwo_admin.php
mv cadarokTwo_List.php cadaroktwo_list.php
mv cadarokTwo_NoFild.php cadaroktwo_nofild.php
mv cadarokTwo_Script.php cadaroktwo_script.php
mv cadarokTwo_ScriptSearch.php cadaroktwo_scriptsearch.php
```

**Phase 3: 코드 참조 수정 (2일)**

1. **ConDb.php 매핑 수정**
```php
// Before
if ($Ttable == "NameCard") {
    $View_TtableB = "NameCard";
}

// After
if ($Ttable == "namecard") {
    $View_TtableB = "namecard";
}
```

2. **각 admin.php 파일 수정**
```php
// Before
$T_TABLE="NameCard";
if($code=="Modify"){include"./namecard_NoFild.php";}
include"NameCard_Script.php";

// After
$T_TABLE="namecard";
if($code=="Modify"){include"./namecard_nofild.php";}
include"namecard_script.php";
```

3. **List.php 파일 수정**
```php
// Before
onClick="location.href='NameCard_admin.php?...'"
onClick="window.open('namecard_ScriptSearch.php?...')"

// After
onClick="location.href='namecard_admin.php?...'"
onClick="window.open('namecard_scriptsearch.php?...')"
```

4. **자동화 스크립트 작성**
```bash
#!/bin/bash
# lowercase_references.sh

# admin.php 파일들에서 include 문장 변경
find /var/www/html/admin/mlangprintauto -name "*_admin.php" -exec \
  sed -i 's/NameCard_Script\.php/namecard_script.php/g' {} \;

find /var/www/html/admin/mlangprintauto -name "*_admin.php" -exec \
  sed -i 's/namecard_NoFild\.php/namecard_nofild.php/g' {} \;

# ... 반복
```

**Phase 4: 테스트 및 검증 (1일)**
```bash
# 1. 로컬 테스트
for product in namecard ncrflambeau merchandisebond littleprint; do
    echo "Testing $product..."
    curl -s "http://localhost/admin/mlangprintauto/${product}_admin.php?mode=form&Ttable=${product}" > /dev/null
    if [ $? -eq 0 ]; then
        echo "✅ $product OK"
    else
        echo "❌ $product FAILED"
    fi
done

# 2. 드롭다운 데이터 테스트
# 3. 수정/저장 기능 테스트
# 4. 전체 워크플로우 테스트
```

**Phase 5: 프로덕션 배포 (반나절)**
```bash
# 1. 프로덕션 백업
# 2. FTP 업로드
# 3. 실 서버 테스트
# 4. 문제 발생 시 롤백 준비
```

---

## 📊 비교표

| 항목 | 완전 소문자 | 매핑 테이블 | 현상 유지 |
|------|------------|------------|----------|
| 작업량 | 🔴 많음 (5일) | 🟡 중간 (2일) | 🟢 없음 |
| 근본 해결 | 🟢 완전 해결 | 🟡 우회 | 🔴 미해결 |
| 유지보수성 | 🟢 최상 | 🟡 보통 | 🔴 나쁨 |
| 확장성 | 🟢 최상 | 🟡 보통 | 🔴 나쁨 |
| 리스크 | 🟡 중간 | 🟢 낮음 | 🔴 높음 |
| 장기 가치 | 🟢 10년+ | 🟡 5년 | 🔴 계속 문제 |

---

## 🎯 실행 결정 가이드

### 즉시 실행 추천 (완전 소문자 통일)
**조건:**
- ✅ 시스템이 안정적으로 작동 중
- ✅ 5일 정도 작업 시간 확보 가능
- ✅ 백업 및 롤백 체계 준비됨
- ✅ 장기적 안정성이 중요

### 점진적 실행 추천 (매핑 테이블)
**조건:**
- ✅ 즉시 큰 변경 어려움
- ✅ 단계적 개선 선호
- ✅ 현재 시스템 크게 건드리고 싶지 않음

### 보류 (현상 유지)
**조건:**
- ❌ 시스템이 불안정
- ❌ 백업 체계 없음
- ❌ 긴급한 다른 작업 있음

---

## 🔧 자동화 도구 제안

### rename_to_lowercase.sh (전체 자동화 스크립트)
```bash
#!/bin/bash
# 파일명 소문자 변환 및 참조 업데이트 자동화

BACKUP_DIR="/var/www/html/backup_$(date +%Y%m%d_%H%M%S)"
ADMIN_DIR="/var/www/html/admin/mlangprintauto"

# 1. 백업
echo "Creating backup..."
mkdir -p "$BACKUP_DIR"
cp -r "$ADMIN_DIR" "$BACKUP_DIR/"

# 2. 파일명 변경 (함수)
rename_files() {
    local old_prefix=$1
    local new_prefix=$2

    for file in "${old_prefix}"_*.php; do
        if [ -f "$file" ]; then
            # 파일명에서 _NoFild, _Script 등 추출
            suffix="${file#${old_prefix}}"
            # 소문자 변환
            new_suffix=$(echo "$suffix" | tr '[:upper:]' '[:lower:]')
            new_file="${new_prefix}${new_suffix}"

            echo "Renaming: $file → $new_file"
            mv "$file" "$new_file"
        fi
    done
}

# 3. 참조 업데이트 (함수)
update_references() {
    local old_name=$1
    local new_name=$2

    echo "Updating references: $old_name → $new_name"

    # admin.php 파일들
    find "$ADMIN_DIR" -name "*.php" -exec \
        sed -i "s/${old_name}/${new_name}/g" {} \;
}

# 4. ConDb.php 업데이트
update_condb() {
    echo "Updating ConDb.php..."

    sed -i 's/\$Ttable == "NameCard"/\$Ttable == "namecard"/g' \
        /var/www/html/mlangprintauto/ConDb.php

    sed -i 's/\$View_TtableB = "NameCard"/\$View_TtableB = "namecard"/g' \
        /var/www/html/mlangprintauto/ConDb.php

    # 반복...
}

# 실행
cd "$ADMIN_DIR"

echo "=== Phase 1: Renaming files ==="
rename_files "NameCard" "namecard"
rename_files "NcrFlambeau" "ncrflambeau"
rename_files "MerchandiseBond" "merchandisebond"
rename_files "LittlePrint" "littleprint"

echo "=== Phase 2: Updating references ==="
update_references "NameCard" "namecard"
update_references "NcrFlambeau" "ncrflambeau"
update_references "MerchandiseBond" "merchandisebond"
update_references "LittlePrint" "littleprint"

echo "=== Phase 3: Updating ConDb.php ==="
update_condb

echo "=== DONE ==="
echo "Backup location: $BACKUP_DIR"
echo "Please test thoroughly before deploying to production!"
```

---

## 📌 최종 결론

### 권장 사항: **완전 소문자 통일 (옵션 1)**

**이유:**
1. ✅ **한 번의 고통, 영원한 평화**: 5일 작업으로 향후 10년 문제 없음
2. ✅ **Linux 표준**: 업계 표준 규칙 준수
3. ✅ **명확성**: 누가 봐도 이해 가능한 구조
4. ✅ **확장성**: 새 제품 추가 시 혼란 제로

**실행 시점:**
- 🎯 **지금 바로** (현재 4개 제품 수정 완료 시점)
- 이미 50% 작업 완료 (inserted, envelope, cadarok, sticker는 소문자)
- 나머지 50% (namecard, ncrflambeau, merchandisebond, littleprint) 통일

**예상 효과:**
```
Before (현재):
- 새 제품 추가: "어? 이건 대문자? 소문자? NoFild는 대문자인데 nofild도 있네?"
- 버그 발생: "왜 파일을 못 찾지? 아 대소문자 문제구나..."
- 유지보수: 매번 케이스 확인 필요

After (소문자 통일):
- 새 제품 추가: "무조건 소문자로 하면 돼!"
- 버그 발생: 대소문자 문제는 영원히 사라짐
- 유지보수: 고민 없이 작업 가능
```

---

## 🚀 실행 제안

### 다음 세션 작업 제안:
1. 자동화 스크립트 작성 및 테스트
2. Phase 1 실행 (파일명 변경)
3. Phase 2 실행 (참조 업데이트)
4. 로컬 테스트
5. 문제 없으면 프로덕션 배포

**시작 명령:**
```bash
# 1단계: 백업
tar -czf /tmp/backup_before_lowercase.tar.gz /var/www/html/admin/mlangprintauto

# 2단계: 스크립트 실행
./rename_to_lowercase.sh

# 3단계: 테스트
./test_all_products.sh

# 4단계: FTP 업로드
./upload_to_production.sh
```

**소요 시간:** 5일 (하루 2-3시간씩 작업)
- Day 1: 스크립트 작성 및 백업
- Day 2: 파일명 변경
- Day 3: 코드 참조 업데이트
- Day 4: 로컬 테스트
- Day 5: 프로덕션 배포

---

**작성일:** 2025-11-08
**작성자:** Claude (AI Assistant)
**환경:** WSL2 Ubuntu, PHP 7.4
