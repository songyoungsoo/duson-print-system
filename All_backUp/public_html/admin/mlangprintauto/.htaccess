# PHP OPcache 비활성화 (개발 중)
php_flag opcache.enable Off
php_flag opcache.enable_cli Off

# 브라우저 캐시 비활성화
<FilesMatch "\.(php|html)$">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires 0
</FilesMatch>
