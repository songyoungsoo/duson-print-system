# 가격 수정 과정 - 2025년 11월 8일

## 📋 목차
1. [문제 요약](#문제-요약)
2. [공통 문제 패턴](#공통-문제-패턴)
3. [품목별 수정 내용](#품목별-수정-내용)
4. [핵심 해결 방법](#핵심-해결-방법)
5. [테스트 결과](#테스트-결과)

---

## 문제 요약

관리자 페이지에서 각 제품의 가격 수정 시 다음 문제들이 발생:

### 주요 증상
1. **드롭다운 메뉴 미표시**: 제품 종류, 재질, 규격 등의 선택 항목이 표시되지 않음
2. **DB 연결 오류**: "DB 접속 에러입니다!" 메시지 표시
3. **파일 찾기 실패**: "검색 스크립트 파일이 없습니다" 오류
4. **데이터 저장 실패**: 수정 버튼 클릭 시 데이터베이스에 저장되지 않음

### 근본 원인
1. **테이블명 대소문자 불일치**: 코드에서 `mlangprintauto_NameCard` 사용, 실제 테이블은 `mlangprintauto_namecard`
2. **Include 파일명 불일치**: 동적 변수로 파일 경로 생성 시 대소문자 문제
3. **ConDb.php 매핑 누락**: sticker 제품의 Ttable 매핑이 없음
4. **변수 스코프 문제**: include된 Script 파일에서 필요한 변수가 정의되지 않음

---

## 공통 문제 패턴

### 1. 테이블명 대소문자 문제

#### ❌ 잘못된 코드
```php
$T_TABLE="NameCard";  // 또는 "MerchandiseBond", "NcrFlambeau"
$TABLE="mlangprintauto_${T_TABLE}";  // 결과: mlangprintauto_NameCard (틀림!)
```

#### ✅ 올바른 코드
```php
$T_TABLE="namecard";  // 소문자
$TABLE="mlangprintauto_namecard";  // 직접 지정
```

### 2. Include 파일명 문제

#### ❌ 잘못된 코드
```php
$T_TABLE="NameCard";
if($code=="Modify"){include"./${T_TABLE}_NoFild.php";}  // NameCard_NoFild.php 찾음
<?include"${T_TABLE}_Script.php";?>  // NameCard_Script.php 찾음
```

#### ✅ 올바른 코드
```php
// 실제 파일명과 정확히 일치하도록 하드코딩
if($code=="Modify"){include"./namecard_NoFild.php";}
<?include"NameCard_Script.php";?>
```

### 3. ConDb.php 매핑 문제

#### ❌ 문제 상황
```php
// ConDb.php에 sticker 매핑이 없음
// $Ttable = "sticker"일 때 $View_TtableA, B, C가 설정되지 않음
```

#### ✅ 해결 방법
```php
if ($ListXTtable == "1.5" || $Ttable == "sticker" || $ToTitle == "스티커") {
    $View_TtableA = "1.5";
    $View_TtableB = "sticker";
    $View_TtableC = "스티커";
}
```

---

## 품목별 수정 내용

### 1. 명함 (NameCard)

#### 파일: `/var/www/html/admin/mlangprintauto/NameCard_admin.php`

**문제점:**
- 테이블명: `mlangprintauto_NameCard` (대문자) → 실제 테이블: `mlangprintauto_namecard` (소문자)
- Include 파일: 동적 변수로 생성하여 대소문자 불일치

**수정 내용:**

```php
// Line 36: 테이블명 수정
// ❌ 수정 전
$TABLE="mlangprintauto_${T_TABLE}";

// ✅ 수정 후
$TABLE="mlangprintauto_namecard";

// Line 46: NoFild include 하드코딩
// ❌ 수정 전
if($code=="Modify"){include"./${T_TABLE}_NoFild.php";}

// ✅ 수정 후
if($code=="Modify"){include"./namecard_NoFild.php";}

// Line 137: Script include 하드코딩
// ❌ 수정 전
<?include"${T_TABLE}_Script.php";?>

// ✅ 수정 후
<?include"NameCard_Script.php";?>
```

#### 파일: `/var/www/html/admin/mlangprintauto/NameCard_List.php`

**문제점:**
- 수정 버튼 클릭 시 "NameCard_ScriptSearch.php" 파일을 찾지 못함
- 실제 파일명: `namecard_ScriptSearch.php` (소문자)

**수정 내용:**

```php
// 이중 변수 시스템 도입
$TIO_CODE="namecard";  // ✅ 소문자 (ScriptSearch 파일명과 일치)
$TIO_CODE_ADMIN="NameCard";  // ✅ admin.php 파일명용 (대문자)
$table="mlangprintauto_namecard";  // ✅ 소문자 테이블명

// 모든 버튼 onClick 핸들러 수정
// 수정 버튼 예시:
onClick="location.href='<?=$TIO_CODE_ADMIN?>_admin.php?mode=form&code=Modify&no=<?=$no?>&Ttable=<?=$Ttable?>'"

// 검색 버튼 예시:
onClick="window.open('<?=$TIO_CODE?>_ScriptSearch.php?...');"
```

**데이터베이스 저장 흐름:**

1. **수정 버튼 클릭** → `NameCard_admin.php?mode=form&code=Modify&no=263&Ttable=namecard`
2. **데이터 로드** → `namecard_NoFild.php`에서 기존 데이터 로드
   ```php
   $MlangPrintAutoFild_result = mysqli_query($db, "select * from mlangprintauto_namecard where no='$no'");
   $MlangPrintAutoFild_row = mysqli_fetch_array($MlangPrintAutoFild_result);
   ```
3. **드롭다운 생성** → `NameCard_Script.php`에서 JavaScript 배열 생성
   ```php
   $result = mysqli_query($db, "select * from $GGTABLE where Ttable='$Ttable' and BigNo='0'");
   // $GGTABLE = "mlangprintauto_transactioncate"
   // $Ttable = "namecard"
   ```
4. **저장 버튼 클릭** → `NameCard_admin.php?mode=Modify_ok`
5. **DB 업데이트** → UPDATE 쿼리 실행
   ```php
   UPDATE mlangprintauto_namecard SET
       style='$RadOne',
       Section='$myList',
       quantity='$quantity',
       money='$money'
   WHERE no='$no'
   ```

---

### 2. 양식지 (NcrFlambeau)

#### 파일: `/var/www/html/admin/mlangprintauto/NcrFlambeau_admin.php`

**문제점:**
- 3단계 구조: 구분(style) → 규격(TreeSelect) → 색상재질(Section)
- 테이블명 대소문자 문제
- Include 파일 경로 문제

**수정 내용:**

```php
// Line 35: 테이블명 수정
// ❌ 수정 전
$TABLE="mlangprintauto_${T_TABLE}";

// ✅ 수정 후
$TABLE="mlangprintauto_ncrflambeau";

// Line 45: NoFild include 하드코딩
// ❌ 수정 전
if($code=="Modify"){include"./${T_TABLE}_NoFild.php";}

// ✅ 수정 후
if($code=="Modify"){include"./NcrFlambeau_NoFild.php";}

// Line 148: Script include 하드코딩
// ❌ 수정 전
<?include"${T_TABLE}_Script.php";?>

// ✅ 수정 후
<?include"NcrFlambeau_Script.php";?>
```

#### 파일: `/var/www/html/admin/mlangprintauto/NcrFlambeau_NoFild.php`

**변경사항:** 없음 (이미 올바른 형식)

```php
$MlangPrintAutoFild_result = mysqli_query($db, "select * from mlangprintauto_ncrflambeau where no='$no'");
$MlangPrintAutoFild_row = mysqli_fetch_array($MlangPrintAutoFild_result);

// 3단계 데이터 로드
$MlangPrintAutoFildView_style = $MlangPrintAutoFild_row['style'] ?? '';  // 구분
$MlangPrintAutoFildView_TreeSelect = $MlangPrintAutoFild_row['TreeSelect'] ?? '';  // 규격
$MlangPrintAutoFildView_Section = $MlangPrintAutoFild_row['Section'] ?? '';  // 색상재질
```

#### 파일: `/var/www/html/admin/mlangprintauto/NcrFlambeau_Script.php`

**특징:** 3단계 드롭다운 구조

```php
// 1단계: 구분 (BigNo='0')
$result = mysqli_query($db, "select * from $GGTABLE where Ttable='$Ttable' and BigNo='0'");
// 예: "일반", "특수" 등

// 2단계: 규격 (BigNo=구분의 no, TreeNo 사용)
$result_Two = mysqli_query($db, "select * from $GGTABLE where TreeNo='{$row['no']}'");
// 예: "A4", "B5" 등

// 3단계: 색상재질 (BigNo=구분의 no)
$result_Two = mysqli_query($db, "select * from $GGTABLE where BigNo='{$row['no']}'");
// 예: "백상지", "미색지" 등

// JavaScript에서 2개의 드롭다운 동시 업데이트
function updateList(str) {
    // myList (색상재질) 업데이트
    // myListTreeSelect (규격) 업데이트
}
```

#### 파일: `/var/www/html/admin/mlangprintauto/NcrFlambeau_List.php`

**추가 수정:** 디자인비 컬럼 추가

```php
// Line 136: 헤더에 디자인비 컬럼 추가
<td align=center>디자인비</td>

// Lines 176-178: 데이터 행에 디자인비 표시
<td align=center><font color=white>
<?php $sumr = (float)$row['DesignMoney']; $sumr = number_format($sumr); echo($sumr); ?>원
</font></td>

// 오타 수정: "생제" → "삭제"
```

**데이터베이스 저장 흐름:**

1. **드롭다운 선택**
   - 구분 선택 → onChange 이벤트 → 규격/색상재질 드롭다운 자동 업데이트
2. **저장**
   ```php
   UPDATE mlangprintauto_ncrflambeau SET
       style='$RadOne',           // 구분
       TreeSelect='$myListTreeSelect',  // 규격
       Section='$myList',         // 색상재질
       quantity='$quantity',
       money='$money',
       DesignMoney='$TDesignMoney'
   WHERE no='$no'
   ```

---

### 3. 상품권 (MerchandiseBond)

#### 파일: `/var/www/html/admin/mlangprintauto/MerchandiseBond_admin.php`

**문제점:**
- 종류 드롭다운에 "선택하세요"만 표시
- 후가공 드롭다운 비어있음

**수정 내용:**

```php
// Line 36: 테이블명 수정
// ❌ 수정 전
$TABLE="mlangprintauto_${T_TABLE}";

// ✅ 수정 후
$TABLE="mlangprintauto_merchandisebond";

// Line 46: NoFild include 하드코딩
// ❌ 수정 전
if($code=="Modify"){include"./${T_TABLE}_NoFild.php";}

// ✅ 수정 후
if($code=="Modify"){include"./MerchandiseBond_NoFild.php";}

// Line 137: Script include 하드코딩
// ❌ 수정 전
<?include"${T_TABLE}_Script.php";?>

// ✅ 수정 후
<?include"MerchandiseBond_Script.php";?>
```

**데이터베이스 저장 흐름:**

1. **MerchandiseBond_Script.php 실행**
   ```php
   // 종류 드롭다운 (BigNo='0')
   $result = mysqli_query($db, "select * from mlangprintauto_transactioncate
                                 where Ttable='MerchandiseBond' and BigNo='0'");

   // 후가공 드롭다운 (BigNo=종류의 no)
   $result_Two = mysqli_query($db, "select * from mlangprintauto_transactioncate
                                     where BigNo='{$row['no']}'");
   ```

2. **저장**
   ```php
   UPDATE mlangprintauto_merchandisebond SET
       style='$RadOne',    // 종류
       Section='$myList',  // 후가공
       quantity='$quantity',
       money='$money'
   WHERE no='$no'
   ```

---

### 4. 스티커 (sticker) ⭐ 핵심 수정

#### 문제점 분석

**증상:**
- 스티커종류와 규격 드롭다운이 모두 비어있음
- 다른 제품과 동일한 패턴으로 수정했지만 작동하지 않음

**근본 원인:**
- `ConDb.php`에 **sticker 제품 매핑이 누락**되어 있음
- `$Ttable = "sticker"`일 때 `$View_TtableA`, `$View_TtableB`, `$View_TtableC`가 설정되지 않음
- Script 파일에서 `$Ttable` 변수가 비어있어 데이터를 가져올 수 없음

#### 파일: `/var/www/html/admin/mlangprintauto/sticker_admin.php`

**수정 내용:**

```php
// Line 35: 테이블명 수정
// ❌ 수정 전
$TABLE="mlangprintauto_${T_TABLE}";

// ✅ 수정 후
$TABLE="mlangprintauto_sticker";

// Line 45: NoFild include 하드코딩
// ❌ 수정 전
if($code=="Modify"){include"./${T_TABLE}_NoFild.php";}

// ✅ 수정 후
if($code=="Modify"){include"./sticker_NoFild.php";}

// Line 136: Script include 하드코딩
// ❌ 수정 전
<?include"${T_TABLE}_Script.php";?>

// ✅ 수정 후
<?include"sticker_Script.php";?>
```

#### 파일: `/var/www/html/mlangprintauto/ConDb.php` ⭐⭐⭐

**핵심 수정:** sticker 매핑 추가

```php
// Lines 30-34: sticker 매핑 추가
if ($ListXTtable == "1.5" || $Ttable == "sticker" || $ToTitle == "스티커") {
    $View_TtableA = "1.5";
    $View_TtableB = "sticker";
    $View_TtableC = "스티커";
}
```

**기존 매핑 구조:**
```php
// Line 6: 공통 테이블 정의
$GGTABLE = "mlangprintauto_transactioncate";

// Line 13: Ttable 변수 설정
$Ttable = isset($_GET['Ttable']) ? $_GET['Ttable'] :
          (isset($_POST['Ttable']) ? $_POST['Ttable'] : '');

// Lines 20-104: 각 제품별 매핑
// 0: inserted (전단지)
// 1: msticker (자석스티커)
// 1.5: sticker (스티커) ← 새로 추가!
// 2: NameCard (명함)
// 3: MerchandiseBond (상품권)
// 4: envelope (봉투)
// 5: NcrFlambeau (양식지)
// ... 등
```

**스티커 데이터 구조:**

데이터베이스에서 확인한 스티커 카테고리:
```
1. 스티커종류 (BigNo='0'): 13개
   - 260: 일반사각형 (61개 규격)
   - 301: 원형스티커 (20개 규격)
   - 302: 타원형스티커 (16개 규격)
   - 645: 데드롱(은/투명)스티커 (24개 규격)
   - 646: 사각귀돌이스티커 (12개 규격)
   - 848: 금박스티커 (1개 규격)
   - 907: 자석스티커(전체) (0개 규격)
   - 908: 자석스티커(종이자석) (0개 규격)
   - 915: 롤스티커 별도견적 (1개 규격)
   - 916: 금지 금박 은박 별도견적 (0개 규격)
   - 917: 크라프트지스티커 별도견적 (0개 규격)
   - 922: 롤스티커 별도견적 (0개 규격)
   - 925: 금지그티커 별도견적 (0개 규격)

총 데이터: 149개 (스티커종류 + 규격)
```

#### 파일: `/var/www/html/admin/mlangprintauto/sticker_Script.php`

**변경사항:** 없음 (이미 올바른 형식)

```php
// 스티커종류 배열 생성 (acts)
$result = mysqli_query($db, "select * from $GGTABLE where Ttable='$Ttable' and BigNo='0'");
while($row = mysqli_fetch_array($result)) {
    // acts[0] = new Activity('260', [...])  // 일반사각형
    // acts[1] = new Activity('301', [...])  // 원형스티커
    // ...
}

// 규격 배열 생성 (VL)
$result_Two = mysqli_query($db, "select * from $GGTABLE where BigNo='{$row['no']}'");
while($row_Two = mysqli_fetch_array($result_Two)) {
    // VL[0] = new Activity('260', ['261', '263', ...])  // 일반사각형의 규격들
    // ...
}

// 드롭다운 업데이트 함수
function updateList(str) {
    // 스티커종류 선택 시 해당 규격들을 myList에 채움
}
```

**데이터베이스 저장 흐름:**

1. **페이지 로드** → `ConDb.php` include
   ```php
   $Ttable = $_GET['Ttable'];  // "sticker"
   // ConDb.php에서 매핑 실행
   $View_TtableC = "스티커";  // 화면 표시용
   ```

2. **Script 실행** → `sticker_Script.php`
   ```php
   // $GGTABLE과 $Ttable이 정상적으로 설정됨
   $result = mysqli_query($db, "select * from mlangprintauto_transactioncate
                                 where Ttable='sticker' and BigNo='0'");
   // 13개 스티커종류 로드
   ```

3. **드롭다운 표시**
   - 스티커종류: 13개 옵션
   - 규격: 스티커종류 선택 시 자동 업데이트

4. **저장**
   ```php
   UPDATE mlangprintauto_sticker SET
       style='$RadOne',    // 스티커종류 (예: 260)
       Section='$myList',  // 규격 (예: 261)
       quantity='$quantity',
       money='$money'
   WHERE no='$no'
   ```

---

## 핵심 해결 방법

### 1. 테이블명 통일 규칙

**원칙:** 모든 테이블명은 소문자 사용

```php
// ✅ 올바른 패턴
$TABLE = "mlangprintauto_namecard";      // 명함
$TABLE = "mlangprintauto_ncrflambeau";   // 양식지
$TABLE = "mlangprintauto_merchandisebond"; // 상품권
$TABLE = "mlangprintauto_sticker";       // 스티커
```

### 2. Include 파일 하드코딩

**원칙:** 실제 파일명과 정확히 일치하도록 직접 입력

```php
// ✅ NoFild 파일
if($code=="Modify"){include"./namecard_NoFild.php";}     // 소문자
if($code=="Modify"){include"./NcrFlambeau_NoFild.php";}  // 대문자
if($code=="Modify"){include"./MerchandiseBond_NoFild.php";}
if($code=="Modify"){include"./sticker_NoFild.php";}      // 소문자

// ✅ Script 파일
<?include"NameCard_Script.php";?>          // 대문자
<?include"NcrFlambeau_Script.php";?>       // 대문자
<?include"MerchandiseBond_Script.php";?>   // 대문자
<?include"sticker_Script.php";?>           // 소문자
```

### 3. ConDb.php 매핑 확인

**원칙:** 모든 제품은 ConDb.php에 매핑 필수

```php
// 체크리스트:
// ✅ inserted (전단지)
// ✅ msticker (자석스티커)
// ✅ sticker (스티커) ← 누락되어 있었음!
// ✅ namecard (명함) - Ttable은 "NameCard"로 매핑
// ✅ merchandisebond (상품권) - Ttable은 "MerchandiseBond"로 매핑
// ✅ envelope (봉투)
// ✅ ncrflambeau (양식지) - Ttable은 "NcrFlambeau"로 매핑
```

### 4. 변수 전달 흐름

```
admin.php (수정 페이지)
├─ include ConDb.php
│  ├─ $GGTABLE = "mlangprintauto_transactioncate" 설정
│  ├─ $Ttable 설정 (GET/POST에서)
│  └─ $View_TtableA, B, C 설정 (제품별 매핑)
│
├─ include NoFild.php (수정 시만)
│  └─ 기존 데이터 로드 (SELECT FROM $TABLE WHERE no='$no')
│
└─ include Script.php
   ├─ $GGTABLE 사용 (ConDb.php에서 설정됨)
   ├─ $Ttable 사용 (ConDb.php에서 설정됨)
   └─ JavaScript 배열 생성 (acts, VL)
```

---

## 테스트 결과

### 1. 로컬 테스트 (http://localhost)

#### 명함 (NameCard)
```bash
✅ 드롭다운 표시: 명함종류 (2개 레벨)
✅ 데이터 로드: 정상
✅ 데이터 저장: 정상
```

#### 양식지 (NcrFlambeau)
```bash
✅ 드롭다운 표시: 구분 → 규격, 색상재질 (3개 레벨)
✅ 데이터 로드: 정상
✅ 데이터 저장: 정상
✅ 리스트 표시: 디자인비 컬럼 추가
```

#### 상품권 (MerchandiseBond)
```bash
✅ 드롭다운 표시: 종류, 후가공 (2개 레벨)
✅ 데이터 로드: 정상
✅ 데이터 저장: 정상
```

#### 스티커 (sticker)
```bash
✅ ConDb.php 매핑: 추가 완료
✅ 드롭다운 표시: 스티커종류 (13개), 규격 (최대 61개)
✅ 데이터 로드: 149개 항목 확인
✅ 데이터 저장: 정상
```

### 2. 프로덕션 서버 (http://dsp1830.shop)

**업로드 완료 파일:**
```
✅ mlangprintauto/ConDb.php
✅ admin/mlangprintauto/NameCard_admin.php
✅ admin/mlangprintauto/NameCard_List.php
✅ admin/mlangprintauto/NcrFlambeau_admin.php
✅ admin/mlangprintauto/NcrFlambeau_List.php
✅ admin/mlangprintauto/MerchandiseBond_admin.php
✅ admin/mlangprintauto/sticker_admin.php
✅ admin/mlangprintauto/sticker_Script.php
```

---

## 완료된 품목 현황

### ✅ 이전 완료 (2025-11-07)
1. 전단지 (inserted)
2. 봉투 (envelope)
3. 카다록 (cadarok)
4. 소량인쇄/포스터 (littleprint)

### ✅ 금일 완료 (2025-11-08)
5. 명함 (NameCard)
6. 양식지 (NcrFlambeau)
7. 상품권 (MerchandiseBond)
8. 스티커 (sticker)

### ⏳ 미확인 품목
- 자석스티커 (msticker)
- 기타 품목들

---

## 추가 참고 사항

### 파일명 규칙 정리

| 제품 | admin.php | NoFild.php | Script.php | 테이블명 |
|------|-----------|------------|------------|----------|
| 전단지 | inserted_admin.php | inserted_NoFild.php | inserted_Script.php | mlangprintauto_inserted |
| 명함 | NameCard_admin.php | namecard_NoFild.php | NameCard_Script.php | mlangprintauto_namecard |
| 봉투 | envelope_admin.php | envelope_NoFild.php | envelope_Script.php | mlangprintauto_envelope |
| 양식지 | NcrFlambeau_admin.php | NcrFlambeau_NoFild.php | NcrFlambeau_Script.php | mlangprintauto_ncrflambeau |
| 상품권 | MerchandiseBond_admin.php | MerchandiseBond_NoFild.php | MerchandiseBond_Script.php | mlangprintauto_merchandisebond |
| 스티커 | sticker_admin.php | sticker_NoFild.php | sticker_Script.php | mlangprintauto_sticker |
| 자석스티커 | msticker_admin.php | msticker_NoFild.php | msticker_Script.php | mlangprintauto_msticker |
| 카다록 | cadarok_admin.php | cadarok_NoFild.php | cadarok_Script.php | mlangprintauto_cadarok |
| 소량인쇄 | littleprint_admin.php | littleprint_NoFild.php | littleprint_Script.php | mlangprintauto_littleprint |

### ConDb.php 전체 매핑 구조

```php
// Line 6: 공통 테이블
$GGTABLE = "mlangprintauto_transactioncate";

// Lines 20-104: 제품별 매핑
0  : inserted        → 전단지
1  : msticker        → 스티카 (자석스티커)
1.5: sticker         → 스티커 (신규 추가!)
2  : NameCard        → 명함
3  : MerchandiseBond → 상품권
4  : envelope        → 봉투
5  : NcrFlambeau     → 양식지
6  : cadarok         → 리플렛
7  : LittlePrint     → 소량인쇄
8  : cadarokTwo      → 카다로그
9  : hakwon          → 학원
10 : food            → 음식
11 : company         → 기업체
12 : cloth           → 의류
13 : commerce        → 상업
14 : church          → 교회
15 : nonprofit       → 비영리
16 : etc             → 기타
```

### 데이터베이스 구조

**제품 테이블 (예: mlangprintauto_namecard)**
```sql
CREATE TABLE mlangprintauto_namecard (
    no INT PRIMARY KEY AUTO_INCREMENT,
    style VARCHAR(255),      -- 제품 종류 (transactioncate.no)
    Section VARCHAR(255),    -- 재질/후가공 (transactioncate.no)
    TreeSelect VARCHAR(255), -- 규격 (3단계 구조 시)
    quantity VARCHAR(255),   -- 수량
    money VARCHAR(255),      -- 가격
    DesignMoney VARCHAR(255),-- 디자인비
    POtype VARCHAR(255),     -- 후가공 타입
    ...
);
```

**카테고리 테이블 (mlangprintauto_transactioncate)**
```sql
CREATE TABLE mlangprintauto_transactioncate (
    no INT PRIMARY KEY AUTO_INCREMENT,
    Ttable VARCHAR(255),     -- 제품명 (예: "namecard", "sticker")
    BigNo VARCHAR(255),      -- 부모 카테고리 번호 ('0'이면 최상위)
    TreeNo VARCHAR(255),     -- 3단계 구조 시 사용
    title VARCHAR(255),      -- 카테고리명
    ...
);
```

**데이터 관계:**
```
2단계 구조 (명함, 상품권, 스티커):
└─ BigNo='0' (스티커종류)
   └─ BigNo='{스티커종류.no}' (규격)

3단계 구조 (양식지):
└─ BigNo='0' (구분)
   ├─ TreeNo='{구분.no}' (규격)
   └─ BigNo='{구분.no}' (색상재질)
```

---

## 문제 해결 체크리스트

새로운 제품 추가 시 확인 사항:

- [ ] `admin/mlangprintauto/{제품}_admin.php`
  - [ ] `$TABLE` 변수를 소문자 테이블명으로 직접 지정
  - [ ] `include"./{제품}_NoFild.php"` 파일명 정확히 확인
  - [ ] `include"{제품}_Script.php"` 파일명 정확히 확인

- [ ] `mlangprintauto/ConDb.php`
  - [ ] 해당 제품의 `$Ttable` 매핑 존재 확인
  - [ ] `$View_TtableA`, `B`, `C` 설정 확인

- [ ] 데이터베이스
  - [ ] 테이블명이 소문자인지 확인 (`mlangprintauto_{제품}`)
  - [ ] `mlangprintauto_transactioncate`에 해당 제품 데이터 존재 확인

- [ ] FTP 업로드
  - [ ] 수정된 `admin.php` 업로드
  - [ ] 수정된 `Script.php` 업로드 (필요시)
  - [ ] 수정된 `ConDb.php` 업로드 (매핑 추가 시)

---

## 작성 정보

- **작성일:** 2025년 11월 8일
- **작성자:** Claude (AI Assistant)
- **작업 환경:** WSL2 Ubuntu, PHP 7.4, MySQL
- **서버:** http://dsp1830.shop (프로덕션), http://localhost (로컬)
- **FTP 계정:** dsp1830 / ds701018

---

## 변경 이력

### 2025-11-08
- ✅ 명함(NameCard) 수정 완료
- ✅ 양식지(NcrFlambeau) 수정 완료 + 디자인비 컬럼 추가
- ✅ 상품권(MerchandiseBond) 수정 완료
- ✅ 스티커(sticker) 수정 완료 + ConDb.php 매핑 추가
- ✅ FTP 업로드 완료

### 2025-11-07
- ✅ 전단지(inserted) 수정 완료
- ✅ 봉투(envelope) 수정 완료
- ✅ 카다록(cadarok) 수정 완료
- ✅ 소량인쇄/포스터(littleprint) 수정 완료
