# 명함 시스템 완성본 구현 가이드 (명함틀250927)

## 📋 개요
CSV 데이터를 기반으로 프리미엄 옵션까지 포함한 명함 계산 시스템 완성본을 구현했습니다.
`mlangprintauto_transactioncate` 테이블과 연동하여 다른 품목에도 적용 가능한 체계적인 구조로 설계되었습니다.

## 🎯 핵심 성과
- ✅ 자동 연쇄 드롭다운 시스템 (종류 → 재질 → 수량 → 인쇄면)
- ✅ 프리미엄 옵션 5개 (박, 넘버링, 미싱, 귀돌이, 오시)
- ✅ 실시간 가격 계산 (기본가 + 디자인비 + 프리미엄 옵션)
- ✅ 호버 확대 갤러리 시스템 (6x3 그리드, 페이지네이션)
- ✅ Wall Street 프로페셔널 스타일 UI

---

## 🗃️ 데이터베이스 구조

### 1. 주요 테이블
```sql
-- 카테고리 정보 (명함 종류/재질)
mlangprintauto_transactioncate
├── no (PK)
├── title (명칭)
├── Ttable ('NameCard')
├── BigNo (상위 카테고리 번호)

-- 가격 정보 (수량/인쇄면별)
mlangprintauto_namecard
├── id (PK)
├── style (종류 번호)
├── Section (재질 번호)
├── quantity (수량)
├── POtype (인쇄면: 1=단면, 2=양면)
├── money (인쇄비)
├── DesignMoney (디자인비)
```

### 2. 데이터 흐름
```
종류 선택 → mlangprintauto_transactioncate (BigNo=0)
재질 선택 → mlangprintauto_transactioncate (BigNo=종류번호)
수량/인쇄면 → mlangprintauto_namecard (조합 검색)
```

---

## 🎨 UI/UX 설계 원칙

### 1. Wall Street 프로페셔널 스타일
```css
/* 핵심 컬러 팔레트 */
background: #f7fafc;    /* 메인 배경 */
sections: #edf2f7;      /* 섹션 배경 */
text: #2d3748;          /* 메인 텍스트 */
accent: #d4af37;        /* 프리미엄 강조색 */

/* 폰트 및 간격 최적화 */
max-font-size: 1.1rem;  /* 헤더 최대 크기 */
max-padding: 5px;       /* 최대 패딩 */
max-margin: 5px;        /* 최대 마진 */
line-height: 1.2;       /* 줄간격 축소 */
```

### 2. 컴팩트 폼 그리드
```css
.inline-form-row {
    display: flex;
    align-items: center;
    margin-bottom: 3px;     /* 행간 최소화 */
}

.inline-label {
    width: 60px;
    font-size: 0.9rem;      /* 폰트 크기 축소 */
    padding: 2px 8px;       /* 패딩 최소화 */
}

.inline-select {
    flex: 1;
    padding: 4px 8px;       /* 셀렉트박스 패딩 축소 */
    font-size: 0.9rem;      /* 폰트 크기 축소 */
}
```

---

## ⚙️ 자동 연쇄 드롭다운 시스템

### 1. 핵심 JavaScript 함수
```javascript
// 종류 변경 → 재질 자동 로드
function handleTypeChange(typeValue) {
    const sectionSelect = document.getElementById('Section');

    fetch(`/mlangprintauto/namecard/get_paper_types.php?style=${typeValue}`)
        .then(response => response.json())
        .then(data => {
            // 첫 번째 재질 자동 선택
            if (data.data.length > 0) {
                sectionSelect.value = data.data[0].no;
                handleSectionChange(data.data[0].no);  // 연쇄 트리거
            }
        });
}

// 재질 변경 → 수량 자동 로드
function handleSectionChange(sectionValue) {
    const amountSelect = document.getElementById('MY_amount');

    fetch(`/mlangprintauto/namecard/get_quantities.php?style=${typeValue}&section=${sectionValue}`)
        .then(response => response.json())
        .then(data => {
            // 첫 번째 수량 자동 선택
            if (data.data.length > 0) {
                amountSelect.value = data.data[0].value;
                calculatePrice();  // 즉시 가격 계산
            }
        });
}
```

### 2. API 엔드포인트
```php
// 재질 조회
get_paper_types.php?style={종류번호}

// 수량 조회
get_quantities.php?style={종류번호}&section={재질번호}&potype={인쇄면}

// 가격 계산
calculate_price_ajax.php?MY_type={종류}&Section={재질}&POtype={인쇄면}&MY_amount={수량}&ordertype={편집방식}
```

---

## 💎 프리미엄 옵션 시스템

### 1. 5가지 옵션
```javascript
const premiumOptions = {
    foil: {
        name: '박',
        basePrice: 30000,      // 500매 이하
        unitPrice: 60,         // 초과시 매수당
        types: ['금박무광', '금박유광', '은박무광', '은박유광', '청박유광', '적박유광', '녹박유광', '먹박유광']
    },
    numbering: {
        name: '넘버링',
        basePrice: 60000,      // 500매 이하
        unitPrice: 120,        // 초과시 매수당
        types: ['1개', '2개 (+1000매당 15,000원)']
    },
    perforation: {
        name: '미싱',
        basePrice: 20000,      // 500매 이하
        unitPrice: 25,         // 초과시 매수당
        types: ['1개', '2개 (+1000매당 15,000원)']
    },
    rounding: {
        name: '귀돌이',
        basePrice: 10000,      // 500매 이하
        unitPrice: 24          // 초과시 매수당
    },
    creasing: {
        name: '오시',
        basePrice: 20000,      // 500매 이하
        unitPrice: 25,         // 초과시 매수당
        types: ['1줄', '2줄', '3줄 (+1000매당 15,000원)']
    }
};
```

### 2. 가격 계산 로직
```javascript
function calculateIndividualPrice(optionType, quantity, basePrice500, pricePerUnit) {
    if (quantity <= 500) {
        return basePrice500;
    } else {
        const additionalUnits = quantity - 500;
        return basePrice500 + (additionalUnits * pricePerUnit);
    }
}

// 특수 케이스 (2개, 3줄 옵션)
function calculateSpecialOptions(type, quantity, basePrice) {
    if (type === 'double' || type === '3line') {
        const thousandUnits = Math.ceil(quantity / 1000);
        return basePrice + (thousandUnits * 15000);
    }
    return basePrice;
}
```

---

## 🖼️ 갤러리 시스템 (완성 예정)

### 1. 현재 구현 상태
- ✅ 호버 시 박스 내 1.2배 확대 효과
- ✅ 6x3 그리드 모달 (1200px 중앙)
- ✅ 페이지네이션 (모달 하단)
- ✅ 라이트박스 확대 기능
- ✅ 실제 이미지 로드 (22개 명함 샘플)

### 2. 이미지 소스
```
C:\xampp\htdocs\ImgFolder\namecard\gallery\
├── (주)유일 테크임병상.jpg
├── 24.11.11 프리카다이렉트 오원경본부장 법무법인OL 명함.jpg
├── 408호변애련이주민사회교육명함.jpg
└── ... (총 22개 실제 명함 이미지)
```

---

## 📁 파일 구조

### 1. 핵심 파일
```
mlangprintauto/namecard/
├── index.php                    # 메인 페이지
├── calculate_price_ajax.php     # 가격 계산 API
├── get_paper_types.php          # 재질 조회 API
├── get_quantities.php           # 수량 조회 API
├── get_namecard_images.php      # 갤러리 이미지 API
├── add_to_basket.php           # 장바구니 추가
└── js/
    ├── namecard-compact.js      # 메인 스크립트
    └── namecard-premium-options.js  # 프리미엄 옵션 스크립트
```

### 2. CSS 구조
```
css/
├── namecard-compact.css         # 명함 전용 스타일
├── compact-form.css            # 컴팩트 폼 그리드
├── unified-price-display.css   # 가격 표시 공통
├── additional-options.css      # 프리미엄 옵션
└── gallery-system.css          # 갤러리 시스템
```

---

## 🔧 시행착오 및 해결책

### 1. 데이터베이스 연동 문제
**문제**: 테이블명 대소문자 불일치 (Linux/Windows 호환성)
```php
// ❌ 기존
$query = "SELECT * FROM mlangprintauto_TransactionCate";

// ✅ 해결
$query = "SELECT * FROM mlangprintauto_transactioncate";  // 모두 소문자
```

### 2. 자동 선택 로직 충돌
**문제**: 수동 선택과 자동 선택이 충돌
```javascript
// ✅ 해결: 이벤트 플래그로 구분
let isAutoSelection = false;

function handleTypeChange(typeValue) {
    isAutoSelection = true;
    // 자동 선택 로직
    isAutoSelection = false;
}
```

### 3. 가격 계산 순서 문제
**문제**: 프리미엄 옵션 가격이 기본 가격 계산 전에 호출됨
```javascript
// ✅ 해결: 계산 순서 명확화
function calculatePrice() {
    // 1. 기본 가격 계산
    const baseResult = await calculateBasePrice();
    // 2. 프리미엄 옵션 계산
    const premiumTotal = calculatePremiumOptions();
    // 3. 최종 합산
    const finalTotal = baseResult.total + premiumTotal;
}
```

### 4. UI 스타일 최적화
**문제**: 과도한 여백으로 인한 공간 낭비
```css
/* ✅ 해결: 모든 여백 최소화 */
* {
    margin: 0;
    padding: 0;
}

.compact-style {
    max-padding: 5px !important;
    max-margin: 5px !important;
    font-size: min(0.9rem, 1.1rem) !important;
    line-height: 1.2 !important;
}
```

---

## 🚀 다음 품목 적용 가이드

### 1. 데이터베이스 준비
```sql
-- 1단계: 품목 카테고리 등록
INSERT INTO mlangprintauto_transactioncate
(title, Ttable, BigNo)
VALUES ('전단지', 'Inserted', 0);

-- 2단계: 하위 옵션 등록 (종이, 크기 등)
INSERT INTO mlangprintauto_transactioncate
(title, Ttable, BigNo)
VALUES ('A4', 'Inserted', {상위번호});

-- 3단계: 가격 테이블 생성
CREATE TABLE mlangprintauto_inserted LIKE mlangprintauto_namecard;
```

### 2. API 파일 복제 및 수정
```bash
# 파일 복제
cp namecard/get_paper_types.php inserted/get_paper_types.php
cp namecard/calculate_price_ajax.php inserted/calculate_price_ajax.php

# 테이블명 변경
sed -i 's/NameCard/Inserted/g' inserted/*.php
sed -i 's/mlangprintauto_namecard/mlangprintauto_inserted/g' inserted/*.php
```

### 3. 프리미엄 옵션 커스터마이징
```javascript
// 품목별 프리미엄 옵션 설정
const productPremiumOptions = {
    namecard: ['foil', 'numbering', 'perforation', 'rounding', 'creasing'],
    inserted: ['coating', 'cutting', 'folding'],  // 전단지용
    sticker: ['lamination', 'cutting', 'outline']  // 스티커용
};
```

### 4. CSS 스타일 상속
```css
/* 품목별 CSS 파일 생성 */
@import url('compact-form.css');           /* 공통 폼 스타일 */
@import url('unified-price-display.css');  /* 공통 가격 표시 */

/* 품목별 특화 스타일 추가 */
.inserted-specific {
    /* 전단지 전용 스타일 */
}
```

---

## 📊 성능 및 최적화

### 1. 로딩 속도 최적화
- ✅ 첫 화면 자동 선택으로 즉시 가격 표시
- ✅ AJAX 응답 1초 이내
- ✅ 이미지 지연 로딩 (lazy loading)
- ✅ CSS/JS 파일 압축

### 2. 사용자 경험 개선
- ✅ 로딩 상태 표시
- ✅ 오류 시 친화적 메시지
- ✅ 모바일 반응형 지원
- ✅ 키보드 네비게이션 지원

### 3. 유지보수성
- ✅ 모듈화된 JavaScript 구조
- ✅ CSS 변수 활용
- ✅ 재사용 가능한 컴포넌트
- ✅ 체계적인 파일 구조

---

## 🎯 핵심 성공 요소

### 1. 데이터 중심 설계
- CSV 데이터를 정확히 데이터베이스 구조로 변환
- 기존 `mlangprintauto_transactioncate` 테이블 활용
- 확장 가능한 가격 계산 로직

### 2. 사용자 중심 UX
- 자동 연쇄 선택으로 클릭 수 최소화
- 실시간 가격 피드백
- 직관적인 프리미엄 옵션 인터페이스

### 3. 개발 효율성
- 재사용 가능한 모듈 구조
- 체계적인 오류 처리
- 명확한 코드 문서화

---

## 📝 다음 단계 (갤러리 완성 후 추가 예정)

1. **갤러리 시스템 문서화**
   - 이미지 로드 최적화 과정
   - 6x3 그리드 레이아웃 구현
   - 라이트박스 UX 개선사항

2. **다른 품목 적용 실제 사례**
   - 전단지 시스템 구현
   - 스티커 시스템 적용
   - 공통 컴포넌트 추출

3. **성능 측정 및 최적화**
   - 로딩 시간 벤치마크
   - 메모리 사용량 최적화
   - 데이터베이스 쿼리 최적화

---

*문서 생성일: 2025년 1월 27일*
*버전: 명함틀250927*
*상태: 갤러리 완성 대기 중*