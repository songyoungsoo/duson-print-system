# 🎯 주문 흐름 가격 계산 일관성 구축 - AI 요청 프롬프트

## 📋 프로젝트 개요

두손기획인쇄(dsp1830.shop) PHP 7.4 + MySQL 인쇄 전자상거래 플랫폼에서
**품목선택 → 장바구니 → 주문서 → 주문완료 → 견적서/이메일 → 관리자페이지**까지
모든 단계에서 **동일한 가격/수량/옵션 표시 로직**이 적용되도록 일관성 있는 시스템을 구축해주세요.

### 공식 주문 품목 (9개)

| # | 품목명 | product_type | 폴더명 |
|---|--------|--------------|--------|
| 1 | 전단지 | `inserted` | inserted |
| 2 | 명함 | `namecard` | namecard |
| 3 | 봉투 | `envelope` | envelope |
| 4 | 카다록 | `cadarok` | cadarok |
| 5 | 상품권 | `merchandisebond` | merchandisebond |
| 6 | 스티커 | `sticker_new` | sticker_new |
| 7 | 양식지 | `ncrflambeau` | ncrflambeau |
| 8 | 자석스티커 | `msticker` | msticker |
| 9 | 포스터 | `littleprint` | littleprint |

> **참고**: 리플렛(leaflet)은 주문 품목에서 제외됨

---

## ⚠️ 중요: 품목별 수량 단위 차이

### 🔴 반드시 숙지해야 할 핵심 규칙

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        ★★★ 전단지(inserted)만의 특수성 ★★★                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  ✅ 전단지만 해당:                                                               │
│     • "연" 단위 사용 (1연 = 약 2,000~4,000매, 사이즈별 상이)                      │
│     • 소수점 0.5연 허용 (0.5연, 1연, 1.5연, 2연...)                              │
│     • 괄호 안에 매수 표기 필수: "0.5연 (2,000매)"                                 │
│     • 고객 편의를 위해 연수와 실제 매수를 함께 표시                                │
│                                                                                  │
│  ❌ 다른 모든 품목 (8개):                                                         │
│     • 정수 단위만 사용 (100, 200, 500, 1000...)                                  │
│     • 소수점 0.5 절대 사용 안 함                                                  │
│     • 괄호 매수 표기 불필요                                                       │
│     • 단위: "매", "장", "개", "부" 등                                             │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 품목별 수량 표시 규칙

| 품목 | product_type | 폴더명 | 수량 단위 | 소수점 | 표시 형식 예시 |
|------|--------------|--------|-----------|--------|----------------|
| **전단지** | `inserted` | inserted | 연 | ✅ 0.5 허용 | `0.5연 (2,000매)`, `1연 (4,000매)` |
| 스티커 | `sticker_new` | sticker_new | 매 | ❌ 정수만 | `100매`, `500매` |
| 명함 | `namecard` | namecard | 매 | ❌ 정수만 | `100매`, `200매` |
| 봉투 | `envelope` | envelope | 매 | ❌ 정수만 | `100매`, `500매` |
| 카다록 | `cadarok` | cadarok | 부 | ❌ 정수만 | `100부`, `500부` |
| 포스터 | `littleprint` | littleprint | 장 | ❌ 정수만 | `10장`, `50장` |
| 상품권 | `merchandisebond` | merchandisebond | 매 | ❌ 정수만 | `100매`, `500매` |
| 자석스티커 | `msticker` | msticker | 매 | ❌ 정수만 | `100매`, `500매` |
| 양식지 | `ncrflambeau` | ncrflambeau | 권/매 | ❌ 정수만 | `10권`, `1000매` |

### 전단지 수량 - 계산 불필요! 드롭다운 값 그대로 사용

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    ★★★ 전단지 매수는 계산하지 않음! ★★★                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  드롭다운에 이미 "0.5연 (2,000매)" 형태로 정해진 옵션이 있음                       │
│  → 선택된 값을 그대로 저장하고 표시하면 됨                                        │
│  → 연 ↔ 매 변환 계산 로직 불필요!                                                │
│                                                                                  │
│  예시 드롭다운 옵션:                                                              │
│    <option value="0.5" data-display="0.5연 (2,000매)">0.5연 (2,000매)</option>   │
│    <option value="1" data-display="1연 (4,000매)">1연 (4,000매)</option>         │
│    <option value="2" data-display="2연 (8,000매)">2연 (8,000매)</option>         │
│                                                                                  │
│  저장할 때:                                                                       │
│    quantity = "0.5"              ← 계산용 원본값                                 │
│    quantity_display = "0.5연 (2,000매)"  ← 드롭다운 텍스트 그대로!               │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 전체 주문 흐름도

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         일반 주문 흐름 (shop_temp 사용)                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  [1.품목선택] → [2.장바구니] → [3.주문서] → [4.결제] → [5.완료] → [6.관리자]       │
│       ↓             ↓             ↓           ↓          ↓          ↓           │
│   가격계산기     shop_temp    shop_temp   orderform   이메일발송   orderformtree │
│   JS+PHP API    테이블       표시        생성        주문확인     조회/표시       │
│                                          orderformtree                          │
│                                          저장                                    │
│                                                                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                    견적 주문 흐름 (Quotation_temp 사용) ★★★                      │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  [견적요청] → [관리자검토] → [견적발송] → [고객승인] → [주문처리] → [관리자]       │
│       ↓           ↓            ↓            ↓            ↓           ↓          │
│  Quotation    quoted_price  이메일전송   Quotation   orderform   orderformtree  │
│  _temp 저장   산출          견적서       _temp→      생성        조회/표시       │
│                                          orderformtree                          │
│                                                                                  │
│  ★ 견적은 shop_temp를 거치지 않음! Quotation_temp에서 직접 orderformtree로 이동  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### ⚠️ 중요: 일반주문 vs 견적주문 테이블 차이

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        ★★★ 테이블 사용 구분 ★★★                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  📦 일반 주문 (품목선택 → 장바구니):                                              │
│     • shop_temp 테이블 사용                                                      │
│     • 장바구니에 담고 → 주문서 작성 → 결제                                        │
│     • shop_temp → orderformtree로 데이터 이동                                    │
│                                                                                  │
│  📋 견적 주문 (견적요청 → 승인):                                                  │
│     • Quotation_temp 테이블 사용 (shop_temp 사용 안 함!)                         │
│     • 견적요청 → 관리자 검토 → 견적발송 → 고객승인 → 주문처리                      │
│     • Quotation_temp → orderformtree로 데이터 이동                               │
│                                                                                  │
│  ❌ 견적 주문은 장바구니(shop_temp)를 거치지 않음!                                │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📁 관련 파일 구조

### 핵심 파일 목록
```
/mlangprintauto/
├── {품목}/                          # 9개 품목 폴더
│   ├── index.php                    # 품목선택 + 가격계산
│   └── add_to_basket.php            # 장바구니 추가 처리
│
│   [실제 품목 폴더]
│   ├── inserted/                    # 전단지 ★ (0.5연 허용)
│   ├── sticker_new/                 # 스티커
│   ├── namecard/                    # 명함
│   ├── envelope/                    # 봉투
│   ├── cadarok/                     # 카다록
│   ├── littleprint/                 # 포스터
│   ├── merchandisebond/             # 상품권
│   ├── msticker/                    # 자석스티커
│   └── ncrflambeau/                 # 양식지
│
├── shop/                            # ★ 일반 주문용 (shop_temp 사용)
│   ├── cart_add.php                 # 장바구니 추가 API
│   ├── cart.php                     # 장바구니 목록
│   ├── cart_update.php              # 장바구니 수정 API
│   ├── order.php                    # 주문서
│   ├── order_process.php            # 주문처리 (shop_temp → orderformtree)
│   └── order_result.php             # 주문완료
├── quote/                           # ★ 견적 주문용 (Quotation_temp 사용)
│   ├── request.php                  # 견적요청 폼
│   ├── submit.php                   # 견적요청 처리 → quotations 저장
│   ├── accept.php                   # 견적승인 처리 → Quotation_temp 저장
│   ├── order.php                    # 견적 주문서 (장바구니 없음!)
│   └── order_process.php            # 견적 주문처리 (Quotation_temp → orderformtree)
└── api/
    └── calculate_price.php          # 가격계산 API

/admin/
├── mlangprintauto/
│   └── orderlist.php                # 관리자 주문목록
├── OrderForm/
│   ├── list.php                     # 주문목록
│   └── detail.php                   # 주문상세
└── quotations/                      # ★ 견적 관리
    ├── list.php                     # 견적목록
    ├── detail.php                   # 견적상세
    └── send_quote.php               # 견적발송 (이메일)

/inc/
├── mail.php                         # 이메일 발송 함수
└── price_display.php                # [신규] 공통 가격/수량 표시 함수

/include/
└── PriceCalculator.php              # [신규] 공통 가격계산 클래스
```

### 품목별 폴더 및 product_type 매핑
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  품목        │ 폴더명           │ product_type      │ 수량단위 │ 소수점      │
├─────────────────────────────────────────────────────────────────────────────────┤
│  전단지      │ inserted         │ inserted          │ 연       │ ✅ 0.5 허용 │
│  스티커      │ sticker_new      │ sticker_new       │ 매       │ ❌ 정수만   │
│  명함        │ namecard         │ namecard          │ 매       │ ❌ 정수만   │
│  봉투        │ envelope         │ envelope          │ 매       │ ❌ 정수만   │
│  카다록      │ cadarok          │ cadarok           │ 부       │ ❌ 정수만   │
│  포스터      │ littleprint      │ littleprint       │ 장       │ ❌ 정수만   │
│  상품권      │ merchandisebond  │ merchandisebond   │ 매       │ ❌ 정수만   │
│  자석스티커  │ msticker         │ msticker          │ 매       │ ❌ 정수만   │
│  양식지      │ ncrflambeau      │ ncrflambeau       │ 매       │ ❌ 정수만   │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 일반주문 vs 견적주문 파일 비교
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  구분          │ 일반 주문                    │ 견적 주문                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│  임시 테이블   │ shop_temp                    │ Quotation_temp ★                │
│  장바구니      │ /shop/cart.php               │ ❌ 없음 (장바구니 안 거침)       │
│  주문서        │ /shop/order.php              │ /quote/order.php                 │
│  주문처리      │ /shop/order_process.php      │ /quote/order_process.php         │
│  최종 저장     │ orderformtree                │ orderformtree (동일)             │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ 데이터베이스 테이블 구조

### shop_temp (일반주문 장바구니)
```sql
CREATE TABLE shop_temp (
    idx INT AUTO_INCREMENT PRIMARY KEY,
    session_id VARCHAR(100),
    member_id INT,
    product_type VARCHAR(50),           -- sticker_new, inserted, namecard...
    product_name VARCHAR(100),
    size VARCHAR(50),
    paper VARCHAR(100),
    quantity VARCHAR(50),               -- 원본값: 전단지="0.5", 기타="100"
    quantity_display VARCHAR(100),      -- ★ 전단지="0.5연 (2,000매)", 기타="100매"
    sides VARCHAR(20),                  -- 단면/양면
    options TEXT,                       -- ★ JSON: {"coating":"유광", "folding":"2단"}
    option_price INT DEFAULT 0,         -- ★ 추가옵션 가격 합계
    base_price INT,                     -- 기본 가격
    price INT,                          -- 최종 가격 (base_price + option_price)
    file_path VARCHAR(255),
    created_at DATETIME
);
```

### Quotation_temp (견적주문 전용) ★★★
```sql
-- ★★★ 견적 주문은 shop_temp가 아닌 이 테이블 사용! ★★★
CREATE TABLE Quotation_temp (
    idx INT AUTO_INCREMENT PRIMARY KEY,
    quote_no VARCHAR(50),               -- 견적번호 연결
    session_id VARCHAR(100),
    member_id INT,
    product_type VARCHAR(50),           -- 제품 종류
    product_name VARCHAR(100),          -- 제품명 (예: "견적상품 (전단지)")
    size VARCHAR(50),
    paper VARCHAR(100),
    quantity VARCHAR(50),               -- 원본 수량
    quantity_display VARCHAR(100),      -- ★ 표시용 수량
    sides VARCHAR(20),
    options TEXT,                       -- ★ JSON 형식 옵션
    option_price INT DEFAULT 0,         -- 옵션 가격
    base_price INT,                     -- 기본 가격
    price INT,                          -- 견적 금액 (quoted_price)
    file_path VARCHAR(255),
    status VARCHAR(20) DEFAULT 'pending', -- pending, accepted, ordered, expired
    valid_until DATE,                   -- 견적 유효기간
    created_at DATETIME,
    accepted_at DATETIME                -- 승인 일시
);
```

### orderformtree (주문상세 - 일반/견적 공통)
```sql
CREATE TABLE orderformtree (
    idx INT AUTO_INCREMENT PRIMARY KEY,
    order_no VARCHAR(50),
    product_type VARCHAR(50),
    product_name VARCHAR(100),
    size VARCHAR(50),
    paper VARCHAR(100),
    quantity VARCHAR(50),
    quantity_display VARCHAR(100),      -- ★ 반드시 저장
    sides VARCHAR(20),
    options TEXT,                       -- ★ JSON 그대로 저장
    option_price INT DEFAULT 0,         -- ★ 추가옵션 가격
    base_price INT,
    price INT,
    file_path VARCHAR(255),
    quote_no VARCHAR(50),               -- 견적 연결 (견적주문인 경우)
    proof_status VARCHAR(20) DEFAULT 'pending',
    created_at DATETIME
);
```

### 테이블 관계도
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              테이블 데이터 흐름                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  일반 주문:                                                                      │
│  ┌──────────┐      ┌──────────────┐      ┌───────────────┐                      │
│  │shop_temp │ ───→ │ orderform    │ ───→ │orderformtree  │                      │
│  │(장바구니) │      │ (주문서)     │      │ (주문상세)    │                      │
│  └──────────┘      └──────────────┘      └───────────────┘                      │
│                                                                                  │
│  견적 주문:                                                                      │
│  ┌───────────────┐      ┌──────────────┐      ┌───────────────┐                 │
│  │Quotation_temp │ ───→ │ orderform    │ ───→ │orderformtree  │                 │
│  │ (견적임시)    │      │ (주문서)     │      │ (주문상세)    │                 │
│  └───────────────┘      └──────────────┘      └───────────────┘                 │
│         ↑                                                                        │
│  ┌───────────────┐                                                               │
│  │  quotations   │  ← 견적요청/검토/발송 정보                                     │
│  │ (견적마스터)  │                                                               │
│  └───────────────┘                                                               │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📐 수량 표시 함수 (계산 없이 저장된 값 사용)

### ⭐ 핵심: 드롭다운에서 선택한 값 그대로 저장 & 표시

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         ★★★ 핵심 원칙 ★★★                                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  1. 품목선택 페이지의 드롭다운에 이미 완성된 텍스트가 있음                          │
│     - 전단지: "0.5연 (2,000매)", "1연 (4,000매)"...                              │
│     - 다른품목: "100매", "200매", "500매"...                                     │
│                                                                                  │
│  2. 장바구니 추가 시 드롭다운 텍스트를 quantity_display에 그대로 저장              │
│                                                                                  │
│  3. 이후 모든 페이지에서 quantity_display를 그대로 표시 (재계산 금지!)             │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 전단지 드롭다운 예시
```html
<!-- 전단지(inserted) 수량 선택 - 텍스트에 이미 매수 포함 -->
<select name="quantity" id="quantity" onchange="updateQuantityDisplay(this)">
    <option value="0.5" data-display="0.5연 (2,000매)">0.5연 (2,000매)</option>
    <option value="1" data-display="1연 (4,000매)">1연 (4,000매)</option>
    <option value="1.5" data-display="1.5연 (6,000매)">1.5연 (6,000매)</option>
    <option value="2" data-display="2연 (8,000매)">2연 (8,000매)</option>
    <option value="3" data-display="3연 (12,000매)">3연 (12,000매)</option>
    <option value="5" data-display="5연 (20,000매)">5연 (20,000매)</option>
</select>

<script>
// 장바구니 추가 시 선택된 옵션의 텍스트를 그대로 전송
function addToCart() {
    const select = document.getElementById('quantity');
    const selectedOption = select.options[select.selectedIndex];
    
    const data = {
        quantity: select.value,                           // "0.5"
        quantity_display: selectedOption.dataset.display  // "0.5연 (2,000매)"
        // 또는: selectedOption.text
    };
    // ...
}
</script>
```

### 다른 품목 드롭다운 예시
```html
<!-- 명함, 스티커 등 - 정수만, 괄호 없음 -->
<select name="quantity" id="quantity">
    <option value="100" data-display="100매">100매</option>
    <option value="200" data-display="200매">200매</option>
    <option value="500" data-display="500매">500매</option>
    <option value="1000" data-display="1,000매">1,000매</option>
</select>
```

### PHP 표시 함수 (단순화)
```php
<?php
/**
 * 주문 아이템의 수량 표시
 * 
 * ★★★ 핵심: 저장된 quantity_display를 그대로 반환 ★★★
 * 계산 로직 없음! 드롭다운에서 선택한 값이 이미 완성된 형태임
 */
function getQuantityDisplay($item) {
    // quantity_display가 있으면 그대로 반환 (계산 불필요!)
    if (!empty($item['quantity_display'])) {
        return $item['quantity_display'];
    }
    
    // 혹시 quantity_display가 없는 레거시 데이터의 경우에만 기본값 반환
    // ★ 실제 시스템 품목 코드 사용
    $units = [
        'inserted'        => '연',   // 전단지만 "연" 단위
        'sticker_new'     => '매',   // 스티커
        'namecard'        => '매',   // 명함
        'envelope'        => '매',   // 봉투
        'cadarok'         => '부',   // 카다록
        'littleprint'     => '장',   // 포스터
        'merchandisebond' => '매',   // 상품권
        'msticker'        => '매',   // 자석스티커
        'ncrflambeau'     => '매',   // 양식지
    ];
    
    $unit = $units[$item['product_type']] ?? '개';
    return $item['quantity'] . $unit;
}
```

### JavaScript 표시 함수 (단순화)
```javascript
/**
 * 주문 아이템의 수량 표시
 * 
 * ★★★ 핵심: 저장된 quantity_display를 그대로 반환 ★★★
 */
function getQuantityDisplay(item) {
    // quantity_display가 있으면 그대로 반환!
    if (item.quantity_display) {
        return item.quantity_display;
    }
    
    // 레거시 데이터 대응 - 실제 시스템 품목 코드 사용
    const units = {
        'inserted':        '연',   // 전단지만 "연" 단위
        'sticker_new':     '매',   // 스티커
        'namecard':        '매',   // 명함
        'envelope':        '매',   // 봉투
        'cadarok':         '부',   // 카다록
        'littleprint':     '장',   // 포스터
        'merchandisebond': '매',   // 상품권
        'msticker':        '매',   // 자석스티커
        'ncrflambeau':     '매',   // 양식지
    };
    
    const unit = units[item.product_type] || '개';
    return item.quantity + unit;
}
```

---

## 📊 수량 선택 UI (드롭다운 값 그대로 저장)

### 전단지 수량 선택 (텍스트에 매수 포함)
```html
<!-- 전단지(inserted) - 드롭다운 텍스트 자체가 완성된 형태 -->
<select name="quantity" id="quantity">
    <option value="0.5" data-display="0.5연 (2,000매)">0.5연 (2,000매)</option>
    <option value="1" data-display="1연 (4,000매)">1연 (4,000매)</option>
    <option value="1.5" data-display="1.5연 (6,000매)">1.5연 (6,000매)</option>
    <option value="2" data-display="2연 (8,000매)">2연 (8,000매)</option>
    <option value="3" data-display="3연 (12,000매)">3연 (12,000매)</option>
    <option value="5" data-display="5연 (20,000매)">5연 (20,000매)</option>
    <option value="10" data-display="10연 (40,000매)">10연 (40,000매)</option>
</select>
<!-- ★ 별도 매수 표시 span 불필요! 드롭다운 텍스트 자체에 포함됨 -->
```

### 다른 품목 수량 선택 (정수만)
```html
<!-- 스티커, 명함, 봉투 등 - 정수 + 단위 -->
<select name="quantity" id="quantity">
    <option value="100" data-display="100매">100매</option>
    <option value="200" data-display="200매">200매</option>
    <option value="500" data-display="500매">500매</option>
    <option value="1000" data-display="1,000매">1,000매</option>
    <option value="2000" data-display="2,000매">2,000매</option>
    <option value="5000" data-display="5,000매">5,000매</option>
</select>
<!-- 괄호 표기 없음 -->
```

### 장바구니 추가 시 quantity_display 저장
```javascript
function addToCart() {
    const quantitySelect = document.getElementById('quantity');
    const selectedOption = quantitySelect.options[quantitySelect.selectedIndex];
    
    const data = {
        product_type: productType,
        quantity: quantitySelect.value,                    // "0.5" 또는 "100"
        quantity_display: selectedOption.dataset.display,  // ★ "0.5연 (2,000매)" 또는 "100매"
        // ... 기타 데이터
    };
    
    $.post('/mlangprintauto/shop/cart_add.php', data, function(res) {
        // ...
    });
}
```

---

## 🎨 추가옵션 시스템

### 옵션 종류
| 옵션코드 | 옵션명 | 값 예시 |
|----------|--------|---------|
| coating | 코팅 | 유광, 무광, 벨벳 |
| folding | 접지 | 2단, 3단, 대문, 병풍 |
| scoring | 오시 | 1줄, 2줄, 3줄 |
| cutting | 재단 | 도무송, 톰슨 |
| numbering | 넘버링 | 일련번호 |
| perforation | 미싱 | 1줄, 2줄 |

### 옵션 JSON 형식
```json
{
    "coating": {"value": "유광", "price": 5000},
    "folding": {"value": "2단", "price": 3000},
    "scoring": {"value": "1줄", "price": 2000}
}
```

### 옵션 표시 형식
```
코팅: 유광 (+5,000원)
접지: 2단 (+3,000원)
오시: 1줄 (+2,000원)
─────────────────
추가옵션 합계: 10,000원
```

---

## 📐 공통 계산/표시 함수 생성 요청

### 1. /include/PriceCalculator.php (공통 계산 클래스)
```php
<?php
class PriceCalculator {
    private $pdo;
    private $productType;
    
    // 기본 가격 조회
    public function getBasePrice($size, $paper, $sides, $quantity);
    
    // 추가옵션 가격 계산
    public function calculateOptionPrice($options);
    
    // 총 가격 계산
    public function calculate($formData);
    
    // 옵션 표시 문자열 생성
    public function formatOptionsDisplay($optionsJson);
    
    // 서버 가격 검증
    public function validatePrice($formData, $submittedPrice);
    
    /**
     * 수량 유효성 검사
     * ★ 전단지만 소수점 허용, 나머지는 정수만
     */
    public function validateQuantity($quantity) {
        if ($this->productType === 'inserted') {
            // 전단지: 0.5 단위 허용
            return preg_match('/^[0-9]+(\.[05])?$/', $quantity);
        } else {
            // 다른 품목: 정수만
            return preg_match('/^[0-9]+$/', $quantity);
        }
    }
}
```

### 2. /inc/price_display.php (공통 표시 함수)
```php
<?php
/**
 * 주문 아이템 포맷팅 (모든 페이지에서 동일하게 사용)
 * 
 * ★★★ 핵심 원칙 ★★★
 * quantity_display는 이미 완성된 형태로 저장되어 있음
 * → 계산 없이 그대로 표시!
 */
function formatOrderItem($item) {
    $options = json_decode($item['options'], true) ?: [];
    
    return [
        'name' => $item['product_name'],
        'size' => $item['size'],
        'paper' => $item['paper'],
        'sides' => $item['sides'],
        'quantity' => $item['quantity_display'] ?: $item['quantity'],  // ★ 그대로 사용!
        'options' => formatOptionsArray($options),
        'option_price' => number_format($item['option_price'] ?? 0) . '원',
        'base_price' => number_format($item['base_price'] ?? $item['price']) . '원',
        'price' => number_format($item['price']) . '원'
    ];
}

/**
 * 옵션 배열을 표시용 문자열로 변환
 */
function formatOptionsArray($options) {
    if (empty($options)) return [];
    
    $result = [];
    $optionNames = [
        'coating' => '코팅',
        'folding' => '접지',
        'scoring' => '오시',
        'cutting' => '재단',
        'numbering' => '넘버링',
        'perforation' => '미싱'
    ];
    
    foreach ($options as $key => $opt) {
        if (isset($optionNames[$key])) {
            $result[] = [
                'name' => $optionNames[$key],
                'value' => $opt['value'],
                'price' => number_format($opt['price']) . '원'
            ];
        }
    }
    return $result;
}

/**
 * 총 합계 계산
 */
function calculateOrderTotal($items) {
    return array_sum(array_column($items, 'price'));
}

/**
 * 이메일/견적서용 HTML 테이블 생성
 */
function generateItemsTable($items) {
    $html = '<table>';
    $html .= '<tr><th>상품명</th><th>수량</th><th>옵션</th><th>금액</th></tr>';
    
    foreach ($items as $item) {
        $formatted = formatOrderItem($item);
        
        $optionsStr = '';
        if (!empty($formatted['options'])) {
            $parts = [];
            foreach ($formatted['options'] as $opt) {
                $parts[] = "{$opt['name']}: {$opt['value']}";
            }
            $optionsStr = implode('<br>', $parts);
        }
        
        $html .= "<tr>";
        $html .= "<td>{$formatted['name']}</td>";
        $html .= "<td>{$formatted['quantity']}</td>";  // ★ 저장된 값 그대로!
        $html .= "<td>{$optionsStr}</td>";
        $html .= "<td style='text-align:right'>{$formatted['price']}</td>";
        $html .= "</tr>";
    }
    
    $html .= '</table>';
    return $html;
}
```

---

## 📄 각 페이지별 수정 요청

### 1단계: 품목선택 페이지
**파일**: `/mlangprintauto/{품목}/index.php`

**요구사항**:
- PriceCalculator 클래스 사용
- **전단지**: 수량 변경 시 괄호 매수 실시간 업데이트
- **다른 품목**: 정수 수량만 선택 가능
- 옵션 선택 시 실시간 가격 업데이트
- 장바구니 추가 시 모든 데이터 전송

### 2단계: 장바구니 추가
**파일**: `/mlangprintauto/shop/cart_add.php`

**요구사항**:
```php
// 필수 저장 필드
$data = [
    'product_type' => $_POST['product_type'],
    'product_name' => $_POST['product_name'],
    'size' => $_POST['size'],
    'paper' => $_POST['paper'],
    'quantity' => $_POST['quantity'],
    'quantity_display' => $_POST['quantity_display'],  // ★ 품목별 형식
    'sides' => $_POST['sides'],
    'options' => $_POST['options'],                    // JSON
    'option_price' => (int)$_POST['option_price'],
    'base_price' => (int)$_POST['base_price'],
    'price' => (int)$_POST['price']
];

// ★ 수량 유효성 검사
$productType = $_POST['product_type'];
$quantity = $_POST['quantity'];

if ($productType === 'inserted') {
    // 전단지: 소수점 0.5 허용
    if (!preg_match('/^[0-9]+(\.[05])?$/', $quantity)) {
        die('잘못된 수량입니다.');
    }
} else {
    // 다른 품목: 정수만
    if (!preg_match('/^[0-9]+$/', $quantity)) {
        die('잘못된 수량입니다.');
    }
}
```

### 3단계: 장바구니 표시
**파일**: `/mlangprintauto/shop/cart.php`

**요구사항**:
- `formatOrderItem()` 함수 사용
- 수량: `quantity_display` 필드 그대로 사용
- **전단지**: "0.5연 (2,000매)" 형식으로 표시
- **다른 품목**: "100매" 형식으로 표시
- 재계산 금지

### 4단계: 주문서 생성
**파일**: `/mlangprintauto/shop/order_process.php`

**요구사항**:
```php
// shop_temp → orderformtree 복사 시 quantity_display 반드시 포함
$sql = "INSERT INTO orderformtree (
    order_no, product_type, product_name, size, paper,
    quantity, quantity_display, sides, options, option_price,
    base_price, price, file_path, quote_no
) SELECT 
    ?, product_type, product_name, size, paper,
    quantity, quantity_display, sides, options, option_price,
    base_price, price, file_path, quote_no
FROM shop_temp WHERE session_id = ?";
```

### 5단계: 주문완료 페이지
**파일**: `/mlangprintauto/shop/order_result.php`

**요구사항**:
- `formatOrderItem()` 함수 사용
- orderformtree.quantity_display 그대로 표시
- **전단지**: "0.5연 (2,000매)" 표시
- **다른 품목**: "100매" 표시

### 6단계: 관리자 페이지
**파일**: `/admin/mlangprintauto/orderlist.php`, `/admin/OrderForm/detail.php`

**요구사항**:
- 동일한 `formatOrderItem()` 함수 사용
- quantity_display 그대로 표시
- 품목별 수량 형식 유지

---

## 📧 견적서/이메일 시스템

### ⚠️ 견적 주문 흐름 (Quotation_temp 사용)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                     견적 주문은 shop_temp를 사용하지 않음!                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  1. 고객: 견적요청 → quotations 테이블에 저장                                     │
│  2. 관리자: 검토 후 견적금액 산출 → quotations.quoted_price 업데이트              │
│  3. 관리자: 견적서 이메일 발송                                                    │
│  4. 고객: 견적 승인 → Quotation_temp 테이블에 저장 ★★★                           │
│  5. 고객: 주문 진행 → Quotation_temp → orderformtree로 이동                      │
│                                                                                  │
│  ❌ shop_temp 테이블 사용 안 함!                                                 │
│  ❌ 장바구니 페이지 거치지 않음!                                                  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 견적서 이메일 발송
**파일**: `/admin/quotations/send_quote.php`, `/inc/mail.php`

```php
function sendQuoteEmail($quote_no) {
    $quote = getQuoteInfo($quote_no);
    $specs = json_decode($quote['specifications'], true);
    
    // 수량 표시 (이미 완성된 형태로 저장되어 있음)
    $quantityDisplay = $specs['quantity_display'] ?? $specs['quantity'];
    
    // 옵션 표시
    $options_html = '';
    if (!empty($specs['finishing'])) {
        $options_html = "<tr><th>후가공</th><td>{$specs['finishing']}</td></tr>";
    }
    
    // 견적 승인 URL (Quotation_temp에 저장하는 페이지로 연결)
    $accept_url = "https://dsp1830.shop/quote_accept.php?no={$quote_no}&token=" . generateQuoteToken($quote_no);
    
    $body = "
    <h2>견적서</h2>
    <p>견적번호: {$quote_no}</p>
    <p>유효기간: {$quote['valid_until']}까지</p>
    
    <h3>요청 사양</h3>
    <table>
        <tr><th>제품</th><td>{$quote['product_type']}</td></tr>
        <tr><th>사이즈</th><td>{$specs['size']}</td></tr>
        <tr><th>용지</th><td>{$specs['paper']}</td></tr>
        <tr><th>수량</th><td>{$quantityDisplay}</td></tr>
        {$options_html}
    </table>
    
    <h3>견적 금액</h3>
    <p style='font-size:24px; color:#d00;'>" . number_format($quote['quoted_price']) . "원</p>
    <p>(부가세 포함)</p>
    
    <p style='margin-top:30px'>
        <a href='{$accept_url}' class='btn'>견적 승인 및 주문하기</a>
    </p>
    ";
    
    sendEmail($quote['email'], "[두손기획인쇄] 견적서 (#{$quote_no})", $body);
}
```

### 견적 승인 처리 (Quotation_temp에 저장) ★★★
**파일**: `/mlangprintauto/quote/accept.php`

```php
<?php
// quote_accept.php - 견적 승인 시 Quotation_temp에 저장

$quote_no = $_GET['no'];
$token = $_GET['token'];

// 토큰 검증
if (!validateQuoteToken($quote_no, $token)) {
    die('잘못된 접근입니다.');
}

$quote = getQuoteInfo($quote_no);
$specs = json_decode($quote['specifications'], true);

// 유효기간 체크
if (strtotime($quote['valid_until']) < time()) {
    die('견적 유효기간이 만료되었습니다.');
}

// ★★★ Quotation_temp에 저장 (shop_temp 아님!) ★★★
$sql = "INSERT INTO Quotation_temp (
    quote_no, session_id, member_id, product_type, product_name,
    size, paper, quantity, quantity_display, sides,
    options, option_price, base_price, price, 
    status, valid_until, created_at, accepted_at
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'accepted', ?, NOW(), NOW())";

// 옵션 JSON 생성
$options_json = json_encode([
    'finishing' => ['value' => $specs['finishing'] ?? '', 'price' => 0]
], JSON_UNESCAPED_UNICODE);

$product_name = "견적상품 ({$quote['product_type']})";

$pdo->prepare($sql)->execute([
    $quote_no,
    session_id(),
    $quote['member_id'],
    'quotation',                           // product_type
    $product_name,
    $specs['size'],
    $specs['paper'],
    $specs['quantity'],                    // 원본 수량
    $specs['quantity_display'] ?? $specs['quantity'],  // ★ 표시용 수량
    $specs['sides'] ?? '양면',
    $options_json,
    0,                                     // 옵션 가격 (견적에 포함됨)
    $quote['quoted_price'],                // base_price = 견적금액
    $quote['quoted_price'],                // price = 견적금액
    $quote['valid_until']
]);

// quotations 테이블 상태 업데이트
$pdo->prepare("UPDATE quotations SET status = 'accepted' WHERE quote_no = ?")
    ->execute([$quote_no]);

// 견적 주문 페이지로 이동 (장바구니가 아님!)
header('Location: /mlangprintauto/quote/order.php?quote_no=' . $quote_no);
```

### 견적 주문 처리 (Quotation_temp → orderformtree)
**파일**: `/mlangprintauto/quote/order_process.php`

```php
<?php
// 견적 주문 처리 - Quotation_temp에서 orderformtree로 이동

$quote_no = $_POST['quote_no'];

// 주문번호 생성
$order_no = date('YmdHis') . rand(1000, 9999);

// orderform 생성 (배송정보 등)
$sql = "INSERT INTO orderform (
    order_no, member_id, orderer_name, orderer_phone, orderer_email,
    receiver_name, receiver_phone, postcode, address, address_detail,
    payment_method, total_price, status, memo, created_at
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'pending', ?, NOW())";
// ... 실행

// ★★★ Quotation_temp → orderformtree 이동 ★★★
$sql = "INSERT INTO orderformtree (
    order_no, product_type, product_name, size, paper,
    quantity, quantity_display, sides, options, option_price,
    base_price, price, quote_no, created_at
) SELECT 
    ?, product_type, product_name, size, paper,
    quantity, quantity_display, sides, options, option_price,
    base_price, price, quote_no, NOW()
FROM Quotation_temp 
WHERE quote_no = ? AND status = 'accepted'";

$pdo->prepare($sql)->execute([$order_no, $quote_no]);

// Quotation_temp 상태 업데이트
$pdo->prepare("UPDATE Quotation_temp SET status = 'ordered' WHERE quote_no = ?")
    ->execute([$quote_no]);

// quotations 테이블 상태 업데이트
$pdo->prepare("UPDATE quotations SET status = 'ordered', ordered_at = NOW() WHERE quote_no = ?")
    ->execute([$quote_no]);

// 결제 페이지로 이동
header("Location: /mlangprintauto/shop/payment.php?order_no=$order_no");
```

### 주문확인 이메일 (일반주문 + 견적주문 공통)
**파일**: `/inc/mail.php`

```php
function sendOrderConfirmEmail($order_no) {
    $order = getOrderInfo($order_no);
    $items = getOrderItems($order_no);  // orderformtree에서 조회
    
    // ★ formatOrderItem() 사용하여 일관된 표시
    $items_html = '';
    foreach ($items as $item) {
        $formatted = formatOrderItem($item);
        
        // 견적 상품인 경우 표시
        $quote_badge = '';
        if (!empty($item['quote_no'])) {
            $quote_badge = '<br><small style="color:#0066cc">[견적주문]</small>';
        }
        
        // 옵션 표시
        $options_str = '';
        if (!empty($formatted['options'])) {
            $opt_parts = [];
            foreach ($formatted['options'] as $opt) {
                $opt_parts[] = "{$opt['name']}: {$opt['value']}";
            }
            $options_str = '<br><small style="color:#666">' . implode(', ', $opt_parts) . '</small>';
        }
        
        // ★ quantity_display 그대로 표시
        $items_html .= "<tr>
            <td>{$formatted['name']}{$quote_badge}{$options_str}</td>
            <td>{$formatted['quantity']}</td>
            <td style='text-align:right'>{$formatted['price']}</td>
        </tr>";
    }
    
    $body = "
    <h2>주문이 완료되었습니다</h2>
    <p>주문해 주셔서 감사합니다.</p>
    
    <h3>주문 정보</h3>
    <table>
        <tr><th>주문번호</th><td>{$order['order_no']}</td></tr>
        <tr><th>주문일시</th><td>{$order['created_at']}</td></tr>
        <tr><th>결제금액</th><td><strong>" . number_format($order['total_price']) . "원</strong></td></tr>
    </table>
    
    <h3>주문 상품</h3>
    <table>
        <tr><th>상품명</th><th>수량</th><th>금액</th></tr>
        {$items_html}
    </table>
    ";
    
    return sendEmail($order['orderer_email'], "[두손기획인쇄] 주문완료 (#{$order_no})", $body);
}
```

---

## ✅ 일관성 체크리스트

### 일반주문: 전단지(inserted) 테스트 - shop_temp 사용
| 단계 | 테이블 | 예상 표시 |
|------|--------|-----------|
| 품목선택 | - | 드롭다운: 0.5연 (2,000매) |
| 장바구니 추가 | shop_temp | product_type: `inserted`, quantity_display: **0.5연 (2,000매)** |
| 장바구니 표시 | shop_temp | 수량: **0.5연 (2,000매)** |
| 주문서 | shop_temp | 수량: **0.5연 (2,000매)** |
| 주문처리 | orderformtree | quantity_display 복사됨 |
| 주문완료 | orderformtree | 수량: **0.5연 (2,000매)** |
| 이메일 | orderformtree | 수량: **0.5연 (2,000매)** |
| 관리자 | orderformtree | 수량: **0.5연 (2,000매)** |

### 일반주문: 명함(namecard) 테스트 - 다른 품목
| 단계 | 테이블 | 예상 표시 |
|------|--------|-----------|
| 품목선택 | - | 드롭다운: 200매 |
| 장바구니 | shop_temp | product_type: `namecard`, quantity_display: **200매** |
| 주문완료 | orderformtree | 수량: **200매** |
| 관리자 | orderformtree | 수량: **200매** |

### 일반주문: 카다록(cadarok) 테스트 - "부" 단위
| 단계 | 테이블 | 예상 표시 |
|------|--------|-----------|
| 품목선택 | - | 드롭다운: 100부 |
| 장바구니 | shop_temp | product_type: `cadarok`, quantity_display: **100부** |
| 주문완료 | orderformtree | 수량: **100부** |

### 견적주문 테스트 (Quotation_temp 사용) ★★★
| 단계 | 테이블 | 예상 표시 |
|------|--------|-----------|
| 견적요청 | quotations | 사양 정보 저장 |
| 관리자 견적발송 | quotations | 이메일로 견적서 발송 |
| 고객 견적승인 | **Quotation_temp** ★ | quantity_display 저장 |
| 견적 주문서 | **Quotation_temp** ★ | 수량: 견적 사양대로 |
| 주문처리 | orderformtree | Quotation_temp에서 복사 |
| 주문완료 | orderformtree | 수량: 견적 사양대로 |
| 관리자 | orderformtree | [견적주문] 표시 |

### 옵션 포함 테스트
| 항목 | 예상 표시 |
|------|-----------|
| 옵션 | 코팅: 유광, 접지: 2단 |
| 기본가격 | 50,000원 |
| 옵션가격 | +8,000원 |
| 최종가격 | **58,000원** |

---

## ⚠️ 절대 금지 사항

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            ❌ 절대 하지 말아야 할 것들                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  1. 전단지 외 품목에서 소수점 수량 허용하기                                        │
│     ❌ 명함 0.5매, 스티커 1.5매 → 이런 건 존재하지 않음!                          │
│                                                                                  │
│  2. 전단지에서 괄호 매수 표기 생략하기                                            │
│     ❌ "0.5연" → 고객이 몇 매인지 모름                                            │
│     ✅ "0.5연 (2,000매)" → 명확함                                                │
│                                                                                  │
│  3. 다른 품목에 괄호 표기 추가하기                                                │
│     ❌ "100매 (100매)" → 불필요한 중복                                            │
│     ✅ "100매" → 간결함                                                          │
│                                                                                  │
│  4. 저장된 quantity_display를 무시하고 재계산하기                                 │
│     ❌ DB에 "0.5연 (2,000매)" 저장 → 화면에 "0.5 연" 표시                         │
│     ✅ DB에 저장된 값 그대로 표시                                                 │
│                                                                                  │
│  5. 연 → 매 변환 계산 로직 만들기                                                 │
│     ❌ 드롭다운 값에서 매수를 계산하는 함수 작성                                    │
│     ✅ 드롭다운 텍스트 "0.5연 (2,000매)"를 그대로 저장/표시                        │
│     → 계산이 필요 없음! 드롭다운에 이미 완성된 텍스트가 있음                        │
│                                                                                  │
│  6. 품목별 분기 처리 누락하기                                                     │
│     ❌ 모든 품목에 동일한 로직 적용                                               │
│     ✅ if (productType === 'inserted') 분기 처리                                 │
│                                                                                  │
│  7. 견적 주문에서 shop_temp 사용하기 ★★★                                         │
│     ❌ 견적 승인 → shop_temp에 저장 → 장바구니로 이동                             │
│     ✅ 견적 승인 → Quotation_temp에 저장 → 견적주문 페이지로 이동                  │
│     → 견적은 장바구니를 거치지 않음!                                              │
│                                                                                  │
│  8. 일반주문과 견적주문 테이블 혼동하기                                            │
│     ❌ 일반주문에서 Quotation_temp 조회                                           │
│     ❌ 견적주문에서 shop_temp 조회                                                │
│     ✅ 일반주문: shop_temp → orderformtree                                       │
│     ✅ 견적주문: Quotation_temp → orderformtree                                  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 작업 순서

1. **DB 스키마 확인/수정**
   - shop_temp에 quantity_display, options, option_price 컬럼 확인
   - **Quotation_temp 테이블 존재 확인 및 동일 컬럼 확인** ★
   - orderformtree에 동일 컬럼 확인

2. **공통 함수 생성**
   - /include/PriceCalculator.php
   - /inc/price_display.php
   - quantity_display 그대로 사용하는 로직 (계산 없음)

3. **품목선택 페이지 수정** (9개 품목)
   ```
   /mlangprintauto/inserted/index.php          ← 전단지 ★ (0.5연 허용)
   /mlangprintauto/sticker_new/index.php       ← 스티커
   /mlangprintauto/namecard/index.php          ← 명함
   /mlangprintauto/envelope/index.php          ← 봉투
   /mlangprintauto/cadarok/index.php           ← 카다록
   /mlangprintauto/littleprint/index.php       ← 포스터
   /mlangprintauto/merchandisebond/index.php   ← 상품권
   /mlangprintauto/msticker/index.php          ← 자석스티커
   /mlangprintauto/ncrflambeau/index.php       ← 양식지
   ```
   - **전단지(inserted)만**: 연 단위 선택 + 괄호 매수 표시 (드롭다운 텍스트 그대로)
   - **다른 8개 품목**: 정수 단위 선택만

4. **일반주문 - 장바구니 관련 파일 수정** (shop_temp 사용)
   - cart_add.php: quantity_display 저장
   - cart.php: quantity_display 표시
   - order_process.php: shop_temp → orderformtree 이동

5. **견적주문 관련 파일 수정** (Quotation_temp 사용) ★★★
   - quote/accept.php: Quotation_temp에 저장 (shop_temp 아님!)
   - quote/order.php: Quotation_temp 조회하여 표시
   - quote/order_process.php: Quotation_temp → orderformtree 이동

6. **이메일 발송 함수 수정**
   - sendOrderConfirmEmail(): formatOrderItem() 사용
   - sendQuoteEmail(): 견적 정보 + quantity_display 표시

7. **관리자 페이지 수정**
   - orderlist.php: orderformtree 조회 (일반/견적 공통)
   - quotations/list.php: 견적 목록 관리

8. **테스트**
   - **일반주문**: 전단지(inserted) 0.5연 주문 전체 흐름 테스트
   - **일반주문**: 명함(namecard) 200매 주문 전체 흐름 테스트
   - **견적주문**: 견적요청 → 승인 → 주문완료 흐름 테스트 ★
   - 이메일 내용 확인

---

## 📝 추가 요청사항

위 내용을 바탕으로:
1. 먼저 현재 코드 구조를 분석해주세요
2. **일반주문(shop_temp)과 견적주문(Quotation_temp)이 분리되어 있는지 확인해주세요** ★
3. 전단지와 다른 품목의 수량 처리가 분리되어 있는지 확인해주세요
4. quantity_display 필드가 모든 단계에서 사용되고 있는지 확인해주세요
5. 누락된 부분이나 불일치하는 부분을 찾아서 수정해주세요
6. 단계별로 수정 코드를 제공해주세요
